<!-- work-id: KAP-0070 -->

_Track: product proof · Area: serialized sandbox · Created: 2026-07-30_

# Prove One Serialized Public Sandbox

Status: active staged implementation and acceptance packet; Contract Correction (Gate 0) and Gate 1
Slices 1–4 are complete and accepted. Slice 3 includes the focused authority, retirement-order,
bounded-I/O, and fail-closed-policy corrections. The focused Slice 2 runner-hardening follow-up and
Slice 4 fixed authority staging/dispatch/cleanup/collection boundary are complete and accepted at
`bc2d6b5`. Gate 1 Slice 5's fixed backup/restore contract correction was independently accepted and
authorized only its offline contract-first implementation and finite evidence lanes. Implementation
then exposed one Rust crate-privacy contradiction in the required gateway-schema compatibility
identity. The canonical owner now freezes the previously ambiguous gateway-literal order and one
no-input digest-only unsupported workspace bridge; fresh independent review accepted that minimal
correction with no blocker, high, or medium finding. State-root implementation subsequently exposed
a second canonical contradiction: successful adoption necessarily adds the state lock and two empty
backup-reference tables, while the owner had said only deployment/readiness changed and no other
byte could change. The owner now replaces that impossible physical-byte claim with a closed semantic
migration: complete read-only predecessor and retained-authority validation precedes mutation; only
the lock, two empty tables, deployment record, and readiness record may be added; no generic Service
recovery or orphan cleanup may run. It also records stock SQLite's same-controller pathname limit
rather than claiming descriptor-relative confinement against the trusted controller authority. Fresh
independent review accepted this focused correction with no blocker, high, or medium finding. Fresh
pre-implementation review then exposed a third canonical contradiction: a capability-free controller
could not acquire the backup-root lock inside the backup-owned `0700` root. The deployment owner now
freezes one exact non-resident command bootstrap that pins the fixed roots and universal lock order
before releasing a capability-free controller phase and the operation-specific backup helper; it
forbids content access in the bootstrap, a reusable privileged broker, caller-selected descriptors,
an alternate launch route, or surviving bootstrap authority. Focused independent review accepted the
correction with no blocker, high, medium, or low finding. The implementation now keeps ordinary
builds on fixed production identities, isolates the unprivileged architecture behind one separately
named fixed test executable, performs validation-first exact migration, publishes receipts without a
two-link crash state, keeps state composition behind one deep package entry, and correlates the
complete runner crash inventory with its durable record. Focused tests, warning-denied Clippy,
formatting, and independent rereview passed with no blocker, high, or medium finding. The already
green Service-reference automaton and accepted gateway digest bridge remain preserved. The first
restore vertical is complete at `c9b1a68`: after the source guard is released, one private restore
guard acquires the exclusive destination-parent lock before the shared backup lock, independently
validates the selected clean generation and exact pending SQLite semantics, rejects clock rollback
and reserved destinations before mutation, reconstructs exact controller ownership, and atomically
installs only canonical `restore.incomplete` at `installed`. Ordinary state open remains denied
after locks are released; source and selected backup bytes remain unchanged. Focused
backup/state-root tests, warning-denied Clippy, formatting, the full default gate, and fresh
independent review passed with no blocker, high, or medium finding. Planning the next transition
exposed a fourth canonical contradiction: the owner required atomic restore-record replacement and
crash retry while its exact state-root inventory permitted no temporary file. The deployment and
testing owners now freeze one controller-owned `restore.state.tmp`, its exact-prefix recovery rule,
the file-fsync/rename/state-root-fsync matrix, and a separate non-ready two-record final-readiness
prefix because POSIX rename cannot change both bytes and filename atomically. The correction is
accepted at `406b848`. The next vertical is accepted at `237a9e9`: restart takes the exclusive
parent, destination-state, and shared backup locks in universal order; reopens and pins the exact
`installed` clean prefix; validates copied pending ownership; atomically converts it to selected
`current`; and advances through partial/complete temporary and both SQLite/rename crash sides to
canonical `stopped`. Retry is idempotent, ordinary open remains denied, and source and selected
backup bytes remain unchanged. Focused backup/state-root tests, warning-denied Clippy, formatting,
the full default gate, and fresh independent review passed with no blocker, high, medium, or low
finding after the review's lock-order finding was corrected. The clean `stopped -> expired` vertical
is accepted at `0b4af3b`: restore reuses the concrete public expiry/tombstone transaction with true
before/after-commit crash barriers while omitting generic orphan cleanup, derives its sole expiry
time from the validated canonical record's frozen `started_at`, and advances only after commit
through partial/complete temporary, rename, and state-root-fsync sides to canonical `expired`. The
empty retained set remains empty without an invented run, tombstone, receipt, authority reference,
or unrelated row change; ordinary open remains denied, retry converges idempotently, and source and
selected backup bytes remain unchanged. Focused backup and ordinary-retention regressions,
warning-denied Clippy, formatting, the full default gate, and fresh correctness/security review
passed with no blocker, high, medium, or low finding after the security review's second-clock
finding was corrected. The clean `expired -> receipts` vertical is accepted at `7ea5b42`: restart
reopens the exact canonical clean `expired` prefix, validates the empty receipt-publication fixed
point through the stopped-only private service without generic initialization, migration, recovery,
or orphan cleanup, and advances through partial/complete `restore.state.tmp`, rename, and
state-root-fsync sides to canonical `receipts`. Every before/after convergence and record prefix
keeps the canonical empty receipt, run, authority, lifecycle, result, cleanup, and event state
unchanged; ordinary open remains denied, retry converges idempotently, and source and selected
backup bytes remain unchanged. Focused backup/receipt regressions, warning-denied all-target Clippy,
formatting, the complete default gate, and fresh correctness/security review passed with no blocker,
high, medium, or low finding. The clean `receipts -> runner` vertical is accepted at `f39b107`:
restart reopens the exact canonical clean `receipts` prefix, runs the concrete descriptor-bound
semantic runner inventory validation, and establishes the same-operation reconciliation fixed point
by proving that no active run or operation exists. It does not open `Application`, launch a runner,
or attempt a provider operation. Every before/after reconstruction and reconciliation side and every
record prefix preserves the empty runner, journal, outbox, run, report, receipt, event, cleanup,
authority, and lifecycle state. Partial and complete `restore.state.tmp`, rename, and retry after
rename converge idempotently to canonical `runner`; ordinary open remains denied and source and
selected-backup bytes remain unchanged. Focused backup and runner-host regressions, warning-denied
all-target/all-feature Clippy, formatting, the complete default gate, and fresh correctness/security
review passed with no blocker, high, medium, or low finding. The clean `runner -> lease` vertical is
accepted at `c3dc353`: restart reopens the exact canonical clean `runner` prefix and establishes the
concrete lease-input publication path's empty fixed point through the stopped-only private service.
It proves there is no run, tombstone, authority-collection owner, backup-authority owner, or
dispatch reference and deliberately does not read or adopt ambient staged `current`. Because the
clean vector has no run, operation, lease epoch, or pinned private authority, no concrete lease
filesystem publication seam is applicable and the transition invents no dispatch directory or input.
Every before/after fixed-point side, partial and complete `restore.state.tmp`, rename, state-root
fsync, and idempotent retry preserves the empty runner, lease, authority, lifecycle, report,
receipt, event, and cleanup state. The focused test removes ambient staged `current`, preserves the
staged generation manifest, keeps ordinary open denied, and preserves destination, source, and
selected backup bytes. Focused backup regressions, warning-denied all-target/all-feature Clippy,
formatting, the complete default gate, and fresh correctness/security review passed with no blocker,
high, medium, or low finding. This vertical does not claim generation replacement, restore
readiness, any nonclean lifecycle reconstruction, actual nonempty lease republication, cleanup
resumption, authority or trust transfer, the production launcher/helper, Linux identity evidence, or
the complete Slice 5 matrix. Offline private backup-generation and production command/helper
implementation may continue under the corrected owner. KAP-0069 selected one serialized reshape.
This packet may produce offline evidence without external authority. It does not authorize a
provider credential, resource, spend, image push, endpoint, DNS, private live command, or public
traffic; each live stage requires the explicit gate below.

Problem: the fixed sandbox API, deterministic service, real `Application`, separate runner handoff,
and cleanup semantics are accepted offline, but the prior Kubernetes-hosted controller topology is
superseded and no live property is proved. The smallest continuation must run one active operation
at a time from one native controller host into one dedicated synthetic cluster, preserve every
product-essential security and recovery property, and delete the removed remote-controller surface
rather than keeping two deployment routes.

Direct owners before implementation: [KAP-0069 decision](KAP-0069.md),
[public sandbox API](../docs/SANDBOX_API.md), [sandbox deployment](../docs/SANDBOX_DEPLOYMENT.md),
[architecture](../docs/ARCHITECTURE.md), [threat model](../docs/THREAT_MODEL.md),
[privacy](../docs/PRIVACY.md), [testing](../docs/TESTING.md), and [build](../docs/BUILD.md).
KAP-0038 remains the higher owner for authorization, mutation-attempt, receiver-result, `UNKNOWN`,
receipt, and inspection semantics.

Depends on: completed [KAP-0069](KAP-0069.md), accepted KAP-0051 and KAP-0052 contracts/service, and
directly reusable [KAP-0055](KAP-0055.md) runner handoff. Accepted KAP-0053 evidence may be used
only according to KAP-0069's evidence map. KAP-0053's controller/stager deployment route is not a
fallback.

## Finite result

One exact clean revision exposes the unchanged fixed `v1` API through a same-origin boundary. A
visitor can choose `healthy` or `unavailable-image`, receive one durable run identity, reconnect to
the same ordered projection, cross an intentional native runner-process loss, observe no blind
second mutation, retrieve the exact frozen receipt, and observe cleanup separately.

The deployment permits at most one active run, including recovery and cleanup. It contains no
customer workload, customer data, production credential, arbitrary Kubernetes input, second
operation, generic provider/storage/queue seam, account system, dashboard, or resident product
interface.

## Selected topology

```text
same-origin edge or reverse proxy
  -> native controller host
       -> owner-private admission SQLite and immutable receipt directory
       -> serial scheduler, global stop, retention, and UID-safe cleanup
       -> per-run least-privilege runner process
            -> owner-private gateway journal and fixed read-only composition
            -> KAP-0055 authenticated report handoff
            -> real kapsel Application
                 -> dedicated synthetic Kubernetes cluster
                      -> one policy-complete run namespace at a time
```

The controller host is one bounded deployment unit, not a generic control plane. Admission/receipt
state has one writer. Scheduler, retention, and cleanup use local concrete `Service` transitions and
separate fixed Kubernetes credentials; no controller-state listener, controller TLS authority,
`TokenReview`, projected controller token, shared SQLite mount, or Kubernetes key-stager workload
exists.

The runner uses a distinct OS identity, one fresh per-run journal/outbox directory, fixed read-only
request/grant/trust/receipt-signing/Kubernetes/handoff inputs, and no admission database or receipt-
store access. Its credential may patch only its exact UID/owner/resource-version/current-image
matched Deployment through the accepted conditional rule. The target workload receives no runner,
controller, key, store, or cleanup authority.

Serialization is a hard isolation and cost control. Capacity remains held until Kapsel is terminal
or `not_attempted`, exact report/receipt handoff is complete where applicable, and every recorded
cluster object is absent. A subsequent run cannot dispatch while cleanup is incomplete. An operator-
owned canary namespace and retained-state fixtures replace simultaneous visitor-run adversarial
checks; they do not weaken metadata, API, network, volume, key, store, receipt, or unrelated-object
denials.

## Ordered work and gates

### Contract Correction (Gate 0)

No external authority is allowed.

1. Amend the deployment, architecture, threat, privacy, testing, and build owners to name the
   serialized topology, one-active limit, temporal isolation, host process/state/key boundary,
   superseded controller route, exact backup unit, and remaining non-claims.
2. Keep the public `v1` request, response, replay, receipt, error, expiry, and disclosure bytes
   unchanged. If exact consumer fixtures would change, stop and return to KAP-0069 rather than
   silently revising `v1`.
3. Delete or make build-unreachable the superseded Kubernetes-hosted system/controller/stager
   composition, controller-state codecs/transport/adapters, exact nine-slot runner-Pod inventory,
   stale GKE fixtures, and old provider commands. Preserve history in Git and preserve only negative
   invariants that apply to the selected route.
4. Lock one implementation map from each retained essential property to an owner, production path,
   deterministic test, and later live assertion. Prove the root package still builds after deleting
   `kapsel-sandbox` and preserve the one-way dependency.

Gate 0 proof-map status (the deployment owner's
[full map](../docs/SANDBOX_DEPLOYMENT.md#gate-0-preservation-and-live-proof-map) is canonical):

| KAP-0069 property                          | Owner and selected path                                                | Gate 0 proof                                              | Later assertion                                     | Status                                         |
| ------------------------------------------ | ---------------------------------------------------------------------- | --------------------------------------------------------- | --------------------------------------------------- | ---------------------------------------------- |
| Identity/replay/receipt                    | API + deployment: retained service on one host volume                  | contract, service, package boundary                       | Gate 1 crash; Gate 3 restore; Gate 4 reconnect      | Gate 0 accepted; host volume planned           |
| Server-owned fixed authority               | API + deployment: fixed staged inputs and role credentials             | fixtures, service/handoff negatives                       | Gate 1 descriptors; Gate 3/4 denials/scenarios      | Gate 1 Slice 4 accepted; live custody unproved |
| Conditional `Application` and exact result | KAP-0038 + deployment: real application/exact patch                    | fixtures, service, handoff, root deletion                 | Gate 1 patch; Gate 3/4 result proof                 | Gate 0 accepted; live admission unproved       |
| Runner loss/reconcile                      | KAP-0055 + deployment: OS runner/journal/handoff                       | handoff process seams                                     | Gate 1 fencing; Gate 3/4 kills                      | Gate 0 accepted; host fencing planned          |
| Bounds                                     | API + deployment: queue 32, active 1 through cleanup, finite host/cost | fixtures/service; topology-neutral preservation           | Gate 1 resources; Gate 3 cost/burst; Gate 4 ceiling | Gate 0 accepted; active=1 planned              |
| Durable stop                               | deployment + threat: local state/roles                                 | retained service stop; topology-neutral preservation      | Gate 1 restart; Gate 3/4 outage/stop                | Gate 0 accepted; host continuity planned       |
| Disclosure/temporal isolation              | privacy + threat + deployment: OS/path/canary/prior-run denial         | fixture/service/handoff negatives                         | Gate 1 model; Gate 3 adversary; Gate 4 disclosure   | Gate 0 accepted; enforcement planned           |
| Cleanup/backup/recreation                  | deployment + privacy: UID inventory and one backup unit                | retained cleanup/retention; topology-neutral preservation | Gate 1 restore; Gate 3 twice; Gate 4 recovery       | Gate 0 accepted; backup composition planned    |
| Separate facts                             | KAP-0038 + API + deployment: distinct durable transitions              | exact fixtures/service/handoff bytes                      | Gate 1 crash; Gate 3 failures; Gate 4 consumer      | Gate 0 accepted; later composition unproved    |

Gate 0 passed its focused deterministic gates, full repository gate, and fresh architecture/security
review with no duplicate active route, weakened KAP-0038 meaning, missing replacement control, or
implicit provider authorization. The exact public `v1` fixtures remained unchanged; the superseded
source, artifacts, commands, and nine-slot model are absent; and the root package deletion boundary
passed.

### Serialized Composition Proof (Gate 1)

No provider account, credential, resource, push, endpoint, DNS, or traffic is allowed.

1. Set the active-run maximum to one while preserving durable admission, idempotency, bounded queue,
   source rate control, saturation, deadlines, global stop, reconnect, expiry, and cleanup-held
   capacity.
2. Compose scheduler, retention, and cleanup as explicit local roles over one owner-private service
   state. Prove no remote state endpoint or generic database operation remains.
3. Launch the accepted KAP-0055 runner under a separate OS identity and per-run directory. Prove
   owner/mode/no-follow checks, fixed inputs, stale-process and stale-lease denial, process loss
   before invocation, after durable invocation, after `apply_started`, after terminal report, and on
   both sides of receipt publication.
4. Render one complete fixed cluster policy and exact conditional mutation admission rule. Freeze
   the dedicated cluster, runtime, CNI, namespace, service-account, RBAC, quota, limit, network,
   image, target, canary, cleanup, and object-count assertions without selecting a provider.
5. Implement host-owned fixed key staging for separate authorization, receipt, tombstone, and trust
   inputs. Prove exact schema, source/destination identity, mode/owner/no-follow checks, no
   arguments, environment, logs, diagnostics, state, backups, or public disclosure, plus outage and
   rotation behavior.
6. Define one crash-consistent backup unit covering admission state, immutable receipts, the one
   active journal when present, capacity, exact ownership inventory, deployment metadata, and
   required public trust. Prove writer fencing, one runnable journal, same identity, stale deletion
   before readiness, and every KAP-0038/publication/cleanup restore seam deterministically.
7. Produce one clean reproducible image and host bundle with authenticated inputs, vulnerability
   results, exact digests, supported architecture, rollback bytes, and no provider identity.

Gate 1 passes only from a clean revision through exact offline tests and independent review. A
passing image or fixture is not deployment authorization.

#### Gate 1 Slice 1 acceptance

Slice 1 replaces the historical eight-active behavior with exactly one durable active reservation
from dispatch through terminal or `not_attempted` handoff and complete cleanup absence. Reopen and
dispatch fail closed on multiple, noncanonical, inconsistent, or missing capacity ownership rows.
The concrete process-local scheduler recovers active work before FIFO dispatch and respects foreign
leases; the retained periodic retention process now uses its concrete role; and cleanup selection,
start, coalesced retry, one durable 15-minute escalation, and exact-absence completion remain
bounded `Service` transitions. No remote state endpoint, generic database operation, controller
protocol, provider, cluster, credential, or live act was added.

The focused `test-sandbox-serialized-capacity` lane crosses active saturation, corrupt-state reopen
and dispatch, restart recovery, FIFO ordering, terminal handoff, cleanup
start/failure/retry/escalation, wrong and present absence evidence, and release only after exact
UID/owner absence. The exact `v1`, service, KAP-0055 handoff, preservation, and package-deletion
lanes remain unchanged. Runner OS identity and process fencing are accepted in Slice 2; Kubernetes
cleanup observation remains Slice 3.

#### Gate 1 Slice 2 acceptance

The accepted Slice 2 boundary pins one fixed input root and generation root, opens twelve fixed
regular inputs descriptor-relatively with exact controller owner/group and `0400` mode, reopens each
for same-inode checks, and rejects root substitution before creating a generation. It transfers each
individual read-only input through one fixed `SCM_RIGHTS` message with identity-only bootstrap
metadata, clears composition arguments/environment, and gives the runner no controller database,
immutable-receipt, backup, prior-journal, cleanup, unrestricted-key path, or public-trust authority.
On Linux one fixed C helper preserves Rust `unsafe_code = "forbid"` while establishing exact
real/effective/saved UID/GID, empty groups, descriptor closure, parent-death, and `no_new_privs`
before Rust runtime; a private cgroup-v2 generation supplies descendant fencing.

The vertical boundary compiles and its containerless focused tests pass on the macOS development
host, including parent/mode/lease rejection, all three retained native outcome paths, three
pre-terminal loss seams, and terminal/report-byte preservation across service reopen without a
host-replacement claim. The exact pinned, privileged-private-cgroup, network-disabled x86-64
Debian/Linux lane executes rather than skipping and passes the fixed helper, descriptor, identity,
cgroup, descendant-failure, durable-allocation/reopen/migration, outcome, full retained
process-loss, and deterministic both-sided receipt-publication matrix. The production controller
fails closed before runner launch until durable policy-complete verification exists, while
already-invoked recovery remains allowed. Slice 3 review later exposed a cross-run retained-state
hole: a new run could inherit the prior fenced journal/outbox. The correction binds retained state
to exact run/operation identity before launch side effects and uses a durable retirement intent so
restart completes interrupted journal/outbox retirement without relaunching a terminal run.

#### Gate 1 Slice 3 contract

Slice 3 is frozen by the deployment owner's provider-neutral `sandbox-policy-v3` model before code.
It defines one 11-object immutable baseline, one two-object operator canary, at most 13 live
run-owned objects, and at most 64 append-only run UID rows under a 31-object modeled live ceiling.
The run namespace has no Service or egress allowance; one fixed runtime and one network-enforcement
identity have no fallback. Exact provisioner, runner, cleanup, and target authorities, the complete
namespace inventory and quota, immutable workload digests, accepted server defaults, conditional
patch preconditions, controller-stopping orphan propagation followed by deterministic child-first
cleanup, and cleanup-epoch plus durably issued latest-attempt absence proof are closed in
`docs/SANDBOX_DEPLOYMENT.md`.

Implementation must derive policy evidence from bounded deterministic Kubernetes responses rather
than caller-supplied digests, reject every omission or widening before the Slice 2 runner host or
`Application`, preserve KAP-0038's single conditional patch and observation-only recovery, and
release the one capacity reservation only after exact Kubernetes absence plus runner revocation,
process/cgroup absence, journal handoff, and explicit same-run-bound fenced-state retirement. The
focused `test-sandbox-cluster-policy` lane proves this offline composition. It cannot prove live
runtime, CNI, RBAC, admission, metadata, or network enforcement and authorizes no provider,
registry, cluster, credential, image, endpoint, DNS, or traffic action.

#### Gate 1 Slice 3 correction and acceptance

The original Slice 3 revision compiled the frozen provider-neutral baseline, canary, run inventory,
network/admission records, exact conditional Deployment comparison, generated ReplicaSet/Pod UID
inventory, and one concrete UID-preconditioned Kubernetes cleanup observer into the existing sandbox
module. Policy verification atomically closes provisioning and binds baseline/canary UIDs plus
Deployment UID/resource-version/image. Cleanup durably binds the latest exact request-list digest,
issues every delete before observation, converges on Namespace absence, and releases capacity only
after all runner-retirement and exact-absence prerequisites.

Slice 3 review also found and repaired a latent Slice 2 cross-run isolation defect. Retained
journal/outbox state binds exact run and operation before staging or launch, and host retirement can
converge from a durable `retiring` record. A later fresh review reopened acceptance because the
service committed revocation before retirement intent in a separate transaction, the returned delete
plan exposed mutable request authority, the concrete Kubernetes path had only post-deserialization
size checks and no request/attempt deadlines, embedded policy parsing could substitute empty values,
and production still exposed caller-composed pre-delete observations.

The focused correction must make the delete plan private, bind execution to its recomputed canonical
bytes, and expose one closed cleanup attempt that refreshes generated children, obtains exact
pre-delete facts, issues only the durable plan, and performs the fresh post-plan observation. One
service transaction must commit revocation, process absence, journal handoff, verifier clearing, and
`runner_state_retiring`; recovery, assignment, and launch must all reject revoked, retiring, or
retired authority. Every Kubernetes DELETE/GET/LIST has a ten-second deadline, the whole attempt has
a 30-second deadline, and every response body is capped at 2 MiB before kube deserialization. Static
behavior-record and digest construction errors fail initialization or verification rather than
becoming empty policy. The caller-assembled summarized provisioning verifier is test-only or
removed.

Acceptance requires focused regressions for mutated-plan rejection before a request, crash recovery
from atomic retirement intent with no relaunch, revoked/retiring/retired recovery and assignment
denial, sequential prior-run capacity/state retirement before fresh run/generation state,
slow/trickled/oversized Kubernetes responses and total-attempt exhaustion, and malformed embedded
policy rejection. The ordinary `ControllerRole::run_once` entry must converge a sole retiring active
run before `SchedulerRole::run_once` for both an unexpired and expired prior lease, without changing
lease epoch, staging new handoff authority, or launching a runner. The deterministic in-process
application adapter must identify its service-only retirement completion as simulated host behavior;
only `ControllerRole` composes actual `RunnerHost::retire` in production.

The correction removes public cleanup observations/plans/requests and the summarized provisioning
route. `KubernetesCleanupRole` binds one fixed client at construction and owns selection, generated-
child refresh, pre-delete observation, durable private-plan derivation, digest recheck, exact
deletes, fresh post-plan observation, completion, and coalesced failure. One SQL statement now
commits runner revocation, process absence, journal handoff, verifier clearing, and retirement
intent; recovery, assignment, application readiness, and launch each reject revoked, retiring, or
retired authority. The client caps HTTP response bodies before kube deserialization and every
request/attempt has an explicit deadline. Embedded record parsing and digest construction propagate
failure. `ControllerRole::run_once` first queries the sole active run and converges retiring or
retired state before scheduler recovery can mint authority; more than one active row fails closed.
The ordinary production entry regression crosses this ordering with both an unexpired and expired
lease while preserving the epoch, staged handoff authority, invocation/report count, operation
identity, and frozen receipt and retiring the retained generation.

Focused cluster-policy, runner-host, runner-handoff, service-contract, Clippy, formatting, tidy,
links, package, the network-disabled pinned Linux runner-host lane, and the full default gate pass.
Fresh correction review reports no blocker or high finding. No provider, credential, resource,
image, endpoint, DNS, traffic, kind, or live-cluster action was used; live enforcement remains
unproved.

Post-acceptance cleanup removed duplicate cleanup selection, kept policy diagnostics private, and
bounded generation-root recovery and retirement. The
[deployment contract](../docs/SANDBOX_DEPLOYMENT.md) and [testing owner](../docs/TESTING.md) state
the exact behavior and proof. These changes do not reopen Slice 3 acceptance or authorize Slice 4,
provider use, or live work.

#### Focused Slice 2 runner-hardening follow-up

Before any Slice 4–6 implementation, re-review the accepted native pre-exec boundary against an
adversarial privileged parent and define the inputs that Slice 6 must bind into the exact final
bundle. This follow-up must:

1. require final runner securebits `0` and effective, permitted, inheritable, ambient, and bounding
   capability sets `0` before the Rust runner receives authority;
2. reject any `security.capability` value on either the helper or pinned runner executable before
   launch;
3. extend the mandatory pinned Linux lane across the finite matrix below rather than inferring
   capability state from UID/GID and `no_new_privs`;
4. state precisely that the private mount namespace and `/run/kapsel-sandbox` bind mount do not hide
   the remaining host filesystem or provide hard filesystem isolation; and
5. decide in the deployment and threat owners whether a finite seccomp, Landlock, or equivalent
   syscall/path restriction is required for this synthetic proof. If it is not required, retain the
   unrestricted native syscall/path surface as an explicit non-claim and Gate 3 adversary rather
   than implying enforcement.

The finite Linux matrix has these required cases: the exact production bootstrap capability set; one
unexpected representative bit in each of the effective, permitted, inheritable, ambient, and
bounding sets; unlocked `KEEP_CAPS` and `NO_SETUID_FIXUP` variants; locked `KEEP_CAPS` and
`NO_SETUID_FIXUP` variants; and a nonempty helper then runner `security.capability` value. The
canonical case must reach the all-zero final securebit/capability state. Unexpected unlocked state
must either normalize to those same exact zeros or fail before authority release; locked state that
cannot be normalized and either file-capability case must fail before launch or authority release.
The production controller/helper bootstrap capability sets are frozen as
`effective=permitted=bounding={CAP_CHOWN,CAP_DAC_OVERRIDE,CAP_FOWNER,CAP_KILL,CAP_SETGID,CAP_SETUID,CAP_SETPCAP,CAP_SYS_ADMIN}`
and `inheritable=ambient={}`. The hostile representative is `CAP_NET_RAW`. The fixed helper must
normalize unlocked supersets to that exact bootstrap state, fail on locked `KEEP_CAPS` or
`NO_SETUID_FIXUP`, then reach securebits and all five capability sets exactly zero plus
`no_new_privs=1` before the Rust runner receives authority. Linux capability closure means the
`effective` representative is also permitted and the ambient representative is also permitted and
inheritable; each case remains separately named and verified.

This follow-up defines and tests the bundle input identities now: the exact C-source SHA-256, pinned
Debian compiler/toolchain identity, built helper SHA-256, and runner SHA-256. The Linux lane rejects
source/compiler drift and emits the three byte identities as private test evidence. Exact final
bundle assembly and reproducibility remain Slice 6; they are not a prerequisite for this follow-up.
No seccomp, Landlock, or equivalent restriction is selected here. The runner's remaining native
syscall surface and visibility of the host filesystem outside the private `/run` mount remain an
explicit non-claim and Gate 3 adversary. The accepted descriptor, identity, parent-death, cgroup,
and recovery evidence may remain, but no owner may claim a complete least-privilege process boundary
until this matrix passes.

Any implementation change requires focused regressions, the existing Linux runner-host lane, the
default repository gate, and fresh security review. This documentation change does not authorize
Docker. Before follow-up acceptance, a maintainer must separately authorize only the existing
pinned, network-disabled `cargo make test-sandbox-runner-host-linux` lane; that authorization must
not include kind, provider resources, credentials, image pulls, or live actions. This follow-up
selects no provider and authorizes no credential, resource, image pull, endpoint, DNS, traffic,
kind, or live action.

#### Focused Slice 2 runner-hardening acceptance

The fixed two-stage helper now normalizes the exact bootstrap capability set, rejects executable
file capabilities at every pinned check, drops securebits and all five capability sets to zero, and
sets `no_new_privs` before the Rust runner can receive descriptors. The Rust runner independently
checks the final `/proc/self/status` state. The finite same-source matrix crosses the canonical,
independently named `CAP_NET_RAW`, unlocked/locked securebit, and helper/runner file-capability
cases. The pinned network-disabled Linux lane passed and emitted exact C-source, helper, and runner
digests; focused host/handoff, Clippy, formatting, diff, and the full default repository gate
passed. Fresh security/contract review found no blocker, high, or medium finding after the
privileged-parent xattr race claim was narrowed to final-authority containment. The remaining host
filesystem and native syscall/path surface stay explicit non-claims and Gate 3 adversaries.

This acceptance removes the prerequisite block and explicitly authorizes offline contract-first Gate
1 Slice 4 implementation only. It does not authorize a provider, external credential, Docker, kind,
cluster, resource, image pull, endpoint, DNS, traffic, or live action.

#### Gate 1 Slice 4 fixed-staging contract and progress

The [deployment owner](../docs/SANDBOX_DEPLOYMENT.md#host-owned-key-and-trust-staging) freezes one
closed thirteen-file, six-family staging schema for authorization, receipt, tombstone, Kubernetes
runner and cleanup, handoff, and public trust. Complete monotonic generations activate atomically;
the dispatch transaction pins the exact generation and aggregate manifest digest, and recovery,
cleanup, retained public trust, and tombstones retain that pin across rotation. At most current plus
one referenced prior generation may exist. Missing or invalid authority holds and retries its
dependent transition without `service_failed`, `not_attempted`, invocation, receipt, or any KAP-0038
receiver outcome.

Migration from a pre-Slice-4 database is allowed only under durable stop while drained: no
dispatched or publicly retained run and no tombstone may exist. The migration fails closed rather
than silently assigning unproved legacy authority to the current generation. Private payloads and
per-file secret digests remain outside SQLite, arguments, environment, diagnostics, and committed
evidence. Slice 4A correction now keeps installer and controller-reader identities separate,
finishes an exactly adjacent complete generation renamed before `current`, uses bounded
descriptor-relative recovery/inventory operations, requires HTTPS endpoints, treats the bounded CA
files explicitly as opaque consumer-parsed bytes, and narrows payload reads by authority family
after minimal whole-manifest and inventory validation. Production constructors reject equality of
either staging/controller UID or GID; only unprivileged unit fixtures have a `cfg(test)`
same-identity constructor. Temporary recovery accepts only one exact adjacent canonical name. The
staging module still exposes no caller-selected deletion operation: Service alone proves complete
reference absence and supplies its durably recorded exact noncurrent identity.

Independent review accepted Slice 4A after crash recovery, descriptor-relative operations,
family-scoped reads, identity separation, and bounded temporary-debris corrections. Independent
review also accepted Slice 4B after dispatch/recovery pinning, stopped-and-drained migration,
queued/dispatched expiry, cleanup pin propagation, and malformed-database pre-mutation guards were
corrected. Slice 4C composes the retained fixed reader into controller and retention processes;
subsequent correction removes externally constructible raw scheduler/retention/cleanup authority,
binds every production `Service` to one fixed reader, rejects excessive reader generation inventory,
and replaces the legacy process-wide tombstone digest file with the validated current-plus-retained
staged keyring. Queued expiry uses current; dispatched expiry and existing tombstones require their
exact retained generation before mutation; admission checks both bounded staged keys across
rotation. The raw service/role contract tests now run as crate-internal tests, while black-box
process tests use only the production authority composition.

The corrected Slice 4E candidate reads the exact run-pinned authorization, receipt/public-trust,
runner Kubernetes, and handoff families; derives the server-owned request, grant, trust, namespace,
lease, and credential; fsyncs and atomically renames one complete `dispatch/<run-id>/lease-<epoch>/`
directory; and passes only its opened descriptor to `RunnerHost`. The host has no shared input path,
validates all twelve descriptors before replacement fencing, and old process/cgroup fencing plus
durable retirement precede descriptor-relative dispatch removal. The former runner-input and
handoff-endpoint caller settings are gone.

Cleanup reuses the Service-bound reader and exact durable pin. Direct production-composition tests
prove staged TLS/token use after rotation and that missing old authority performs no cleanup or
event mutation. Retained receipt trust is returned unchanged through a separate authority-controller
channel using the run pin, never through receipt retrieval. Service collection validates all pin
storage, rejects unknown pins and orphan receipt/publication/cleanup/application or dispatch owners,
persists one collection record, and restart-convergently removes only the exact unreferenced
noncurrent generation. Current is never deleted. The fixed focused gates target the moved
crate-internal suites. Obsolete projected-input, digest-key, and unused helper parameters were
removed rather than ignored.

The container-free root-only Linux lane exercised the shipped `stage-authority` and `init` commands
on Debian 13 with controller `65530:65530`, staging `65531:65531`, bounded installer capabilities, a
capability-free reader, and a no-capability installer denial inside isolated network namespaces. The
host lacked `cargo-make`, so root invoked its exact implementation script directly with Cargo
offline; the repository Make task remains its ordinary entry point. Source
`8f52e265310b7867109f5d7c5e209ad6fa9a4c5150c5aeb27bd6a00c3f2cb02b` produced launcher
`286a34c572d4faf6d666f14b6a79582b086c89019c139fb0b546a9dc2bf6371f`. The capability-bearing installer
activated authority, the capability-free controller opened it and initialized state, and the
no-capability installer failed as required. The lane configured no endpoint or traffic and used no
Docker, provider, credential, cluster, image pull, or live resource.

Final end-to-end review corrected the shared-input design, production installer reachability,
dispatch-owner checks, startup collection preflight, cancellation documentation, duplicated Base64
encoding, stale test alias, Linux-only stale tuple bindings, and formatting. The final candidate
passes the complete default offline CI gate, warning-denied all-target Clippy, style audit with zero
warnings, focused collection, cleanup, cluster-policy, serialized-capacity, native-listener, and
runner-handoff suites. Independent final review then accepted the exact Linux evidence and reported
no blocker, high, or medium finding. Slice 4 is accepted at `bc2d6b5`. This acceptance authorizes
only the contract-first offline Slice 5 packet; every provider or live action remains blocked.

#### Gate 1 Slice 5 backup/restore contract correction

The [deployment owner](../docs/SANDBOX_DEPLOYMENT.md#crash-consistent-backup-restore-and-rollback)
selects one quiesced-offline, crash-consistent backup unit. Independent review accepted the initial
contract, and later implementation-driven corrections freeze the gateway-schema digest bridge and
the exact stopped migration additions plus stock-SQLite namespace assumption. Fresh independent
review accepted the latter focused correction with no blocker, high, or medium finding; offline
implementation may resume. The fixed direction is one private deep backup/restore module and the
deployment owner's exact `backup`/`restore` command grammars over one fixed state root and backup
root. It adds no trait, plugin, archive/storage/provider seam, daemon, second database, online
snapshot, caller file list, or cross-host anti-cloning claim.

The capture precondition is durable stop, every finite local controller-authority role quiesced, the
runner/cgroup fenced and absent, SQLite handles closed, and one exclusive fixed lock. It captures
the bounded service SQLite and optional DELETE rollback journal, immutable receipts and at most one
pending publication, zero or one semantic runner recovery unit, closed deployment compatibility, and
at most two public-trust documents. It references exact private authority generations but copies no
private staged bytes, seeds, tombstone keys, Kubernetes/handoff credentials, dispatch inputs, logs,
diagnostics, metrics, or evidence. Backup identity is fixed at `65529:65529`; capture retains only
`CAP_DAC_READ_SEARCH`, while restore retains only `CAP_CHOWN`, `CAP_DAC_OVERRIDE`, and `CAP_FOWNER`
during tree construction. The production Linux lane must prove both sets, empty groups, zero
securebits, and `no_new_privs=1` against the accepted controller/staging/runner identities.

One canonical `kapsel.sandbox.backup.v1` generation binds lexical fixed paths, lengths, SHA-256,
generation/predecessor/current facts, capture time and stop, every schema/policy/architecture/
compatibility identity, Slice 2 source/helper/runner identities, authority references, and public
trust identities. Exact maxima are the deployment owner's 64-MiB service database and journal,
4,096/64-MiB receipts, one pending receipt, one 16-MiB runner database and journal, one 16-KiB
outbox, 16-KiB deployment record, two 1-KiB trust documents, two authority references, 4,112
manifest entries, 256-KiB manifest, 256-MiB generation, two complete generations, and one temporary.
Unknown, noncanonical, oversized, linked, substituted, malformed, incompatible, secret-bearing, or
unowned state fails before publication or restore mutation.

`Service` owns pending, published, replacing, and deleting backup reference records. Authority
collection treats their exact generation/manifest sets as owners. Replacement holds the union until
old bytes are durably removed; deletion removes bytes before releasing references. Restore accepts
one opened generation, exact compatible bundle, separately restored exact authority, one trusted
clock sample not before capture, local process/cgroup absence, and an absent destination. It writes
a new stopped tree and durable incomplete marker, reconstructs semantic runner state with fresh
local identities, then in fixed order validates, applies expiry/tombstones, converges pending
receipts, reconciles the same operation without blind mutation, republishes fresh pinned lease
inputs, resumes cleanup without invented absence, and proves one capacity owner/runnable journal
before readiness. Every serving or mutating command, including `clear-stop`, refuses before
readiness.

The mandatory deterministic gate crosses every capture/reference/publication/deletion fsync, every
restore component and readiness side, and thirteen lifecycle snapshots from admission through
retained tombstone. Only the exact backup-reference and stopped-restore transactions may change
source SQLite; every other source and selected-backup byte remains unchanged. Every failure proves
no partial selection, early serve, second journal/capacity owner, authority rebind, changed receipt/
result, invented absence, or blind apply. A separate root-only network-namespace lane proves the
fixed backup identity and capabilities. Gate 1 cannot prove provider-volume detachment or
destruction of another host; those remain explicit Gate 3 prerequisites.

### Reviewed Live Authorization (Gate 2)

Before requesting any live command, commit one zero-resource fixture naming:

- exact clean source, image, bundle, rendered configuration, dependency, provider plugin, and tool
  digests;
- exact provider candidate, region, Kubernetes/runtime/CNI/storage versions, controller-host shape,
  dedicated-cluster shape, private network/access path, and no public application endpoint;
- distinct controller, runner, cleanup, target, backup, key, and operator identities with exact
  allowed and denied actions;
- state volume, receipt store, per-run journal, key/trust source, backup, restore, rotation,
  deletion, and replacement-host fencing rules;
- the complete owned resource inventory, dependency order, deletion order, per-resource absence
  command, orphan scan, experiment expiration, and named cleanup owner;
- fixed one-active, queue, rate, CPU, memory, storage, connection, event, deadline, retention,
  cleanup-escalation, and worst-case duration bounds;
- every fixed and metered cost class, current official rate source and retrieval time, measured or
  conservative quantity, maximum experiment spend, and teardown reserve;
- exact logs, metrics, audit fields, routing, access, retention, committed evidence, private raw-
  evidence deletion receipt, and proof that no request, locator, receipt, journal, key, workload
  output, or application diagnostic enters provider logs; and
- stop conditions for fallback runtime/policy, identity widening, key export widening, data
  disclosure, storage ambiguity, unaccounted cost, incomplete cleanup, or stale provider facts.

Current provider, Kubernetes, identity, networking, storage, key, logging, and pricing statements
must be re-read from official primary documentation during this gate. The fixture and an independent
review must pass before a maintainer may separately approve the exact account, region, credentials,
resource classes, maximum spend, cleanup owner, experiment expiration, and teardown command. That
approval authorizes only the private Gate 3 experiment.

### Private Live Acceptance (Gate 3)

The cluster has no public application endpoint. Use only the approved operator path and stop on the
first rejection condition.

Prove:

1. exact cold and warm creation, both fixed scenarios, real `Application` receipts, and receipt
   inspection against separately supplied trust;
2. runner kill/replacement at every owned seam with one run/operation identity, contiguous replay,
   no blind second mutation after `apply_started`, exact frozen bytes, and explicit `UNKNOWN` where
   receiver evidence is insufficient;
3. denial from the runner and target against the operator canary, metadata, unrelated namespaces,
   controller API/state, receipts, prior journals, volumes, key sources, cleanup authority, and
   every conditional-patch downgrade;
4. omission or weakening of each runtime, CNI, RBAC, quota, limit, NetworkPolicy, admission, image,
   ownership, and deadline control fails before `Application` invocation;
5. bursts beyond transport, rate, queue, one-active, host, subnet, and cluster bounds; global stop
   during a burst; continued retained reads, recovery, receipt retrieval, expiry, and cleanup;
6. client abandonment, deadline, API outage, stuck finalizer, cleanup-process loss, wrong UID/owner,
   retry, one 15-minute escalation, exact absence, and no next dispatch before capacity release;
7. state/key/receipt interruption, key denial and rotation, backup at every seam, original-writer
   fencing, replacement-host restore, stale-data deletion, and no identity, receipt, or capacity
   duplication;
8. deliberately incompatible bundle/config/policy/key rollout, stop, rollback, retained `v1` reads,
   in-flight reconciliation, and no transport-to-result promotion;
9. exact retention and disclosure inspection across host files, provider records, logs, metrics,
   audit, backups, evidence, and committed bundle; and
10. worst-case duration/resource/cost measurement, complete teardown, zero inventory, clean
    recreation, smoke proof, second teardown, and zero inventory again.

Gate 3 produces only disclosure-reviewed aggregate and synthetic evidence. It authorizes neither a
public endpoint nor DNS/website connection.

### Bounded Public Exposure (Gate 4)

A separate explicit approval must name the exact accepted Gate 3 revision, public endpoint, same-
origin proxy, DNS owner, exposure start/end, admission/rate configuration, spend ceiling, on-call
cleanup owner, global-stop test, and rollback/teardown command.

Before website connection, independently exercise both fixed scenarios, lost admission response,
reconnect, runner kill, receipt retrieval, expiry, saturation, global stop, cleanup
failure/recovery, disclosure, and endpoint rollback through the public boundary. Recheck cost and
abuse bounds under the configured ceiling. Only the exact accepted revision may receive traffic.

Gate 4 acceptance closes this packet. The independent website and Grafik consumers may then connect
to the unchanged fixture-proven API. KAP-0054 becomes eligible to begin its decision; it inherits no
sandbox topology or interface.

## Acceptance

- Canonical owners describe only one active serialized deployment route and explicitly supersede the
  old controller/stager route.
- Public `v1` bytes remain unchanged and all KAP-0051 fixtures pass.
- One active run is enforced durably through terminal handoff and complete cleanup absence.
- The native runner crosses real process loss through KAP-0055 and the real `Application`; no
  timeout, transport, cleanup, animation, or inspection fact changes receiver meaning.
- Host state, runner journal, key/trust inputs, Kubernetes authority, receipts, backups, and cleanup
  have exact owners, least authority, denial tests, restart behavior, retention, and rollback.
- The dedicated cluster proves runtime, CNI, metadata, API, network, conditional-patch, canary, and
  unrelated-resource denial without customer or production data.
- Global stop, saturation, abandonment, restore, stale-data expiry, cleanup escalation, exact
  absence, rollback, teardown, and clean recreation pass from one exact revision.
- Every provider, resource, spend, endpoint, DNS, and traffic act has a prior exact gate approval;
  no earlier packet or fixture supplies it.
- Focused sandbox lanes, `cargo make check`, and fresh architecture/security review pass at each
  contract/code boundary.

## Rollback and deletion test

Before exposure, rollback means activate the global stop, preserve retained reads/recovery/receipt
retrieval/cleanup, restore the last accepted host bundle and compatible state, reconcile the one
active operation, and tear down every owned resource after acceptance or rejection. During public
exposure, endpoint and website rollback are separate from host/state/cluster rollback and cannot
change a receiver result.

Delete and retire the hosted proof if any mandatory property requires concurrent runs, a remote
controller plane, a generic storage/provider/queue seam, broader caller authority, customer data,
production credentials, unbounded key export, an unproved runtime/network boundary, an unowned cost
class, or a larger spend/cleanup window than explicitly approved. Also retire if exact teardown and
clean recreation cannot pass twice. Retirement removes the endpoint and deployment artifacts, keeps
only historical evidence needed to explain the decision, preserves the root v0.2 package, and does
not create another sandbox implementation packet.

## Non-goals

No second capability, arbitrary Kubernetes input, customer cluster, production environment, resident
service, generic control plane, provider/storage/queue interface, public SDK, account, billing
product, dashboard, compatibility promise beyond fixed sandbox `v1`, or inference that a public
proof is production multi-tenancy.

## Next action

Continue Gate 1 Slice 5 through contract-first TDD with only `lease -> cleanup`. Reopen the exact
canonical clean `lease` prefix, resume only the owner-defined cleanup fixed point, cross both sides
of every applicable cleanup transaction/filesystem seam and the frozen restore-record replacement
protocol, and advance to canonical `cleanup` without early ordinary open, invented run or operation,
runner launch, provider attempt, receipt change, authority rebind, invented absence, or unrelated
state change. Continue one named stopped-restore transition at a time before generation replacement,
nonclean lifecycle vectors, the launcher, or shipped commands. Do not copy private gateway DDL into
the sandbox or widen the accepted digest-only workspace bridge. Do not begin any provider,
credential, cluster, image, endpoint, DNS, traffic, Docker, kind, or live action without a new
explicit authorization. Gate 2 remains a separately reviewed decision after all Gate 1 slices are
accepted.

## Residual risk

The controller host is a concentrated compromise and availability boundary. A compromised runner may
misuse its loaded per-run grant, Kubernetes, or receipt-signing authority; a compromised host may
alter admission, projection, cleanup, or key inputs. OS identities are not hard tenant isolation,
and a sandboxed Kubernetes runtime can still contain defects. Anonymous abuse, provider records,
stuck cleanup, fixed infrastructure cost, and external copying of public receipts remain. Passing
this packet proves only one bounded synthetic demonstration, not production safety, customer-hosted
operation, complete capture, exactly-once mutation, or commercial viability.
