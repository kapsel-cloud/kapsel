<!-- work-id: KAP-0045 -->

_Track: technical release · Area: release/evidence · Created: 2026-07-16_

# Rehearse And Publish 0.1.0

Status: complete.

Problem: The `0.1.0` release requires one clean-checkout rehearsal and an auditable release
decision, not merely a collection of individually passing development commands.

Contract owners: [KAP-0038 release acceptance](KAP-0038.md), [technical scope](../docs/SCOPE.md),
[build](../docs/BUILD.md), [testing](../docs/TESTING.md), and [review](../docs/REVIEW.md).

Depends on: [KAP-0044](KAP-0044.md).

## Work

- Record version `0.1.0` and tag `v0.1.0` consistently across workspace, artifacts, and notes.
- Rehearse installation, operator setup, healthy operation, both process-termination recoveries,
  failed rollout, offline inspection, MCP invocation, and cleanup from a clean checkout.
- Run every deterministic, live, integration, documentation, and artifact gate on the exact release
  revision.
- Record commands, results, supported platforms, claim limits, and residual risks in a concise
  release report.
- Verify repository documentation and artifact metadata against the release revision without moving
  private launch work into this repository.
- Create the tag and public release only after an explicit operator go-ahead and a passing
  acceptance review.

## Acceptance

- Every item in KAP-0038 release acceptance has direct evidence from the release revision.
- Release artifacts and checksums match the tagged source and pass clean-environment smoke tests.
- The owned live demo passes and leaves no kind cluster or unbounded failure artifact behind.
- Release notes state supported platforms, command surfaces, prototype format status, non-claims,
  security limits, and residual risks.
- The repository is clean, local and remote history is understood, and publication uses no force
  operation.
- The public release URL and exact revision are recorded after publication.

## Release decision

The final package and artifact version is `0.1.0`, and the intended tag is `v0.1.0`. This is the
stable, non-prerelease artifact for the experiment, not a stable-interface or production-readiness
claim. The sole supported artifact remains `x86_64-unknown-linux-gnu`. This release publishes the
GitHub source tag, archive, checksum, and notes; it does not publish a separate package registry
artifact.

## Release notes

Kapsel `0.1.0` is the stable x86-64 GNU/Linux artifact for one bounded
`kubernetes.set_deployment_image` experiment. It includes the local grant-provisioning, operation,
restart, and offline-inspection commands; one fixed-schema stdio MCP tool; and a disposable-kind
crash-recovery demonstration. Install and evaluator commands are in the bundled `EVALUATOR.md` and
[build guide](../docs/BUILD.md).

The downloadable files are `kapsel-0.1.0-x86_64-unknown-linux-gnu.tar.gz` and its adjacent
`.sha256`. Verify the inner archive before extraction. The checksum detects changed bytes but does
not authenticate the publisher. The archive records its exact source revision, dirty state, target,
builder, license, and binary digests.

The sole supported target is `x86_64-unknown-linux-gnu`. The CLI, configuration, MCP adapter,
receipt format, Rust API, and artifact layout remain experiment interfaces without compatibility
promises. The release does not claim production readiness, exactly-once Kubernetes effects,
Kubernetes truth, causation, complete capture, compliance, another capability, or another platform.

## Preparation evidence

At revision `85d94c8b1121fe3a3ab5368fba72a7834299bdea`, the default gate and hosted run
[`29597641376`](https://github.com/kapsel-cloud/kapsel/actions/runs/29597641376) passed artifact
smoke, byte-for-byte reproducibility, strict clean assembly, and upload. The downloaded `0.1.0`
checksum and metadata matched that exact clean revision. A clean supported x86-64 GNU/Linux host
passed both live-kind proofs plus the source-built and downloaded-artifact process-termination
demonstrations; each reported one apply, `ProgressDeadlineExceeded`, frozen receipt recovery,
offline `INSPECTED`, and removed its owned cluster. This preparation run authorizes the final
exact-revision rehearsal, not publication.

## Rehearsal checklist

- [x] Final version, MCP server version, changelog, archive name, and checksum agree.
- [x] Repository history is understood and the exact final revision is clean.
- [x] `cargo make fmt`, focused Clippy, `./scripts/ci-local.sh`, links, and diff hygiene pass.
- [x] Hosted artifact smoke, reproducibility, strict clean assembly, and upload pass.
- [x] The downloaded checksum verifies and metadata names the exact revision with
      `source_dirty: false`.
- [x] Both explicit live-kind paths pass on the supported target and remove their cluster.
- [x] Source-built and downloaded-artifact process-termination demonstrations pass with one apply,
      frozen receipt bytes, failed-rollout classification, and offline inspection.
- [x] Release notes state commands, supported target, prototype formats, security limits,
      non-claims, and residual risks.
- [x] An acceptance review passes before the explicit publication decision.
- [x] After approval, `v0.1.0`, the public release URL, and attached archive/checksum are recorded.

## Publication evidence

Two independent acceptance reviews found no technical or claim blocker. After explicit operator
approval, annotated tag `v0.1.0` was created without force at exact revision
`3d99ba7d8d46c79109b0a510cc20468fe22d5e5c`. The public
[Kapsel 0.1.0 release](https://github.com/kapsel-cloud/kapsel/releases/tag/v0.1.0) contains the
verified archive and adjacent checksum from hosted run
[`29598671876`](https://github.com/kapsel-cloud/kapsel/actions/runs/29598671876). Downloading the
public attachments again verified the checksum, version, exact source revision, clean state, and
supported target.

## Non-goals

No production-readiness declaration, company launch narrative, adoption claim, second capability,
package registry publication, or future-expansion decision.

## Next action

No 0.1.0 technical release work remains. Keep [KAP-0046](KAP-0046.md) blocked until approved
aggregate evidence supports one explicit future direction.
