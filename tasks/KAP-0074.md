<!-- work-id: KAP-0074 -->

_Track: product preview · Area: customer-resident boundary · Created: 2026-08-13_

# Implement The Minimal Customer-Resident Preview

Status: Slices 1 through 5 accepted after deterministic gates, native-Linux evidence, disposable
privileged qualification, and fresh independent review. Slice 5 repository implementation was
explicitly authorized on 2026-08-16 and accepted on 2026-08-26. Customer, production,
external-installation, artifact, release, commit, and push actions remain separately gated; Slice 6
is not authorized. This packet implements the exact resident inventory selected by
[KAP-0054](KAP-0054.md); it does not permit customer access, credentials, a production workload,
deployment, traffic, release, or wider architecture.

Direct owners: [KAP-0054](KAP-0054.md), [architecture](../docs/ARCHITECTURE.md),
[testing](../docs/TESTING.md), and the
[KAP-0038 experiment boundary](../docs/experiments/KAP-0038-kubernetes-effect-gateway-boundary.md).
KAP-0038 continues to own authorization, lifecycle, receiver result, `UNKNOWN`, and receipt bytes.

## Fixed implementation unit

Add exactly one unpublished `kapseld -> kapsel` package, one `kapseld` executable, one authenticated
Linux Unix-domain socket, one systemd service unit, and one `sysusers.d` file. The existing KAP-0038
SQLite journal remains the sole lifecycle truth. Expected new or substantially rewritten non-test
production code is 1,200–2,000 lines. Crossing 3,000 lines or three weeks stops work and reopens
KAP-0054.

The accepted runtime, path, identity, Kubernetes RBAC, protocol vocabulary, bounds, status
projection, receipt retrieval, lifecycle commands, uninstall retention rule, support window, and
unsupported failure domains are copied by reference from KAP-0054 and may not drift here.

## Caller and authority contract

- The caller supplies only the existing exact five-field request or an operation identity for
  status/receipt retrieval.
- The locked `kapsel` identity exclusively owns the process, operator files, Kubernetes credential,
  grant trust, receipt signing seed, journal, worker lock, and receipt directory.
- Every start descriptor-relatively opens fixed `/etc/kapsel`, `/var/lib/kapsel`, receipt, and
  runtime roots; requires exact service ownership and modes; and validates the operator document,
  grant, public key, kubeconfig, signing seed, optional journal/worker lock, and receipt directory
  without symlink traversal. Authority reads use the validated opened inode. Existing KAP-0038
  opening validates each used receipt file, while frozen resident receipt paths are additionally
  confined to the fixed receipt directory.
- The socket parent and mode gate the exact caller group. Every connection must also pass Linux
  kernel peer-credential validation for the expected effective caller group.
- Host root is trusted; no claim is made against a compromised host administrator or kernel.
- The caller never selects or receives an operator path, credential, grant/trust bytes, signing
  material, journal path, receipt path, lifecycle transition, or Kubernetes patch.
- Kubernetes authority is namespaced `get` and `patch` on the exact Deployment. RBAC is not a field
  policy; the existing concrete adapter remains responsible for the fixed conditional image patch.

## Installation and lifecycle contract

The one supported preview is an exact x86-64 Debian 12 build with systemd and Kubernetes 1.33 for
one 30-day non-production evaluation. Install direct artifacts without a required Python or shell
wrapper, provision the static identities, validate exact file ownership/modes, reload systemd, and
`enable --now kapseld.service`. The unit contract is exactly `User=kapsel`,
`Group=kapsel-preview-callers`, `RuntimeDirectory=kapsel`, `RuntimeDirectoryMode=0750`,
`StateDirectory=kapsel`, `StateDirectoryMode=0700`, and `Restart=no`.

On every permitted activation—enabled boot, explicit `start`, or explicit `restart`—`kapseld` opens
the fixed operator configuration and reconciles the exact configured operation once before admitting
submission. It does not periodically retry, and `Restart=no` prevents an automatic same-boot process
restart loop. Configuration, open, or reconciliation failure remains failed until another explicit
activation or the next enabled boot. Runtime-directory cleanup owns normal socket removal. Before
bind, the process descriptor-relatively removes only an exact service-owned socket in the expected
directory and refuses every other stale identity. Caller disconnect does not cancel an accepted
in-process operation. SIGTERM may interrupt at any durable window; the next start uses existing
KAP-0038 recovery. `start`, `stop`, `restart`, `enable --now`, and `disable --now` are systemd
operations.

Uninstall first runs `disable --now` and waits for process exit and connection closure. The operator
then removes the caller from `kapsel-preview-callers`, revokes the preview Kubernetes credential,
and removes unit, sysusers, executable, and socket artifacts while retaining the locked
account/group, operator files, journal, and receipts. Destructive purge is unsupported. No socket
command performs administration, key management, migration, purge, health, or shutdown.

## Test-first vertical slices

Implement one reviewed vertical slice at a time:

1. **Read-only application seam:** add capability-specific projected status and safe exact frozen
   receipt reading behind `Application`. First prove no Kubernetes access, no lifecycle advancement,
   path/digest/byte validation, non-final behavior, and unchanged CLI/MCP contracts.
2. **Bounded authenticated socket:** prove the four-byte big-endian length/body/required client
   write-half-close grammar, duplicate/unknown/oversized/trailing rejection, peer credential
   allow/deny, one exact effective caller group, disclosure bounds, status vocabulary, receipt hex
   round-trip, an eight-connection cap, two-second frame-read/response-write deadlines,
   idle/incomplete close behavior, and saturation without lifecycle effect before wiring submission.
3. **Execution and disconnect:** compose `Application::execute`; prove one in-flight submission,
   `BUSY` without a queue, caller disconnect without cancellation, concurrent reconnect/status, and
   no second provider attempt.
4. **Startup and process loss:** prove startup reconciliation and real process termination at the
   existing mutation and receipt-publication seams, frozen bytes under restart, and explicit
   `UNKNOWN` preservation.
5. **Clean installation:** in a fresh x86-64 Debian 12 systemd environment, prove identities,
   startup descriptor/path checks, caller denial of every operator file, allowed socket use, exact
   RBAC manifest, one reconciliation per boot/start/restart activation, no automatic same-boot
   restart loop, safe stale-socket handling, secret-free systemd diagnostics, stop-before-revocation
   ordering, and uninstall with retained data.
6. **Exact artifact:** assemble one unpublished preview artifact, install without source changes or
   maintainer access, rerun the complete workflow, and independently review architecture and
   security before any external installation is requested.

Use deterministic local Kubernetes fixtures for default tests. Docker/kind, systemd containers or
VMs, and every credential/customer action remain explicit environment gates. Do not weaken a proof
because the local host is macOS.

## Slice 1 implementation record

The user explicitly authorized only Slice 1 on 2026-08-13. The candidate adds synchronous,
capability-specific `Application` reads for projected status and exact finalized receipt bytes plus
the journal-frozen SHA-256. The application checks the configured grant-bound operation identity
before any journal lookup. Status and receipt reads use the existing journal snapshot and
publication owners, expose no path, make no Kubernetes request, and advance no lifecycle state.

Focused deterministic evidence covers absent, active, pre-attempt rejection, terminal `SUCCEEDED`,
`FAILED`, and `UNKNOWN`, wrong-identity non-disclosure, exact receipt bytes, non-final `NOT_READY`,
persisted-fact coherence, and fail-closed missing, changed, wrong-mode, linked, symlinked, special,
oversized, or digest-mismatched receipt storage. First-review corrections additionally prove that an
existing row must match the current exact grant tuple and authority within one SQLite read view,
receipt-bearing states require a canonical envelope whose key and classifier-valid statement match
the complete journal-derived frozen facts, and relative receipt roots fail before journal access.
Restrictive umask cannot weaken exact `0600` publication, FIFO substitution cannot block receipt
reads, and repeated active status/receipt reads leave the next provider sequence unchanged. Focused
application, journal, receipt, publication, CLI, MCP, process-loss, documentation, formatting, and
complete default gates passed. Three fresh review rounds found and corrected grant isolation,
lifecycle/receipt coherence, restrictive-umask, FIFO, path-race, parser-ownership, and atomic-read
defects; focused independent verification found no remaining blocker, high, or medium finding. No
`kapseld`, socket, systemd, installation, package, dependency, schema, or later-slice surface is
part of this accepted slice.

## Slice 2 authorization record

The user explicitly authorized only Slice 2 on 2026-08-15: the bounded authenticated Linux
Unix-domain socket, read-only status and receipt composition through the accepted `Application`
methods, submit-grammar validation without execution, and a non-destructive Linux peer-credential
allow/deny gate. The authorization does not permit `Application::execute`, successful submission,
startup reconciliation, systemd or identity mutation, installation, credentials, provider or
customer access, deployment, traffic, release, or Slices 3–6.

### Slice 2 exact JSON records

KAP-0054 owns the three request names and transport bounds. This slice fixes the previously missing
concrete JSON records so implementation does not invent a generic envelope or imply submission
acceptance. Object key order is not significant on input; duplicate keys fail closed at every
nesting level. The exact request shapes are:

```json
{"request":"get_set_deployment_image_status","operation_id":"operation-id"}
{"request":"get_set_deployment_image_receipt","operation_id":"operation-id"}
{"request":"submit_set_deployment_image","operation_id":"operation-id","namespace":"demo","deployment":"agent-api","container":"api","immutable_image_digest":"registry.example/agent-api@sha256:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"}
```

Status responses contain only `status`, except `NOT_ATTEMPTED` also contains its required
`target_rejection`. Receipt responses are `{"status":"NOT_FOUND"}`, `{"status":"NOT_READY"}`, or a
ready record containing only `status:"READY"`, `receipt_hex`, and `receipt_sha256`. `receipt_hex` is
lowercase hexadecimal of the exact bytes returned by `Application`, and `receipt_sha256` is the
unchanged journal-frozen lowercase digest. The valid Slice 2 submit record returns exactly
`{"status":"ERROR","error_class":"submission_unavailable"}` and performs no `Application` call,
operation creation, Kubernetes access, or lifecycle transition. Slice 3 replaces this temporary
error only when it separately owns execution, disconnect, and `BUSY` semantics.

After authentication and a complete valid frame, malformed JSON, invalid UTF-8, duplicate, unknown,
missing, null, wrong-typed, trailing, cross-request, or out-of-grammar fields receive exactly
`{"status":"ERROR","error_class":"invalid_request"}`. An `Application` read failure receives exactly
`{"status":"ERROR","error_class":"operation_failure"}`. Peer denial, incomplete or invalid framing,
a missed read or write deadline, trailing framed input, saturation, or an unrepresentable over-limit
response closes without a response. None of these errors reflects input, paths, authority, peer
credentials, low-level diagnostics, or receipt bytes.

## Slice 2 implementation record

The candidate adds one unpublished `kapseld -> kapsel` package and one executable. Its deep private
handler checks Linux kernel peer credentials before reading or allocating a caller body, admits at
most eight owned connections through a non-waiting permit after transport accept, and immediately
closes saturation without reading a body, responding, or calling `Application`. It applies one
aggregate two-second frame deadline including required write-half-close, parses one strict
capability-specific JSON record, and calls only the Slice 1 status or receipt read. The synchronous
read runs through Tokio's bounded blocking boundary behind one standard-library mutex so it cannot
block another connection's reactor-owned frame deadline. The handler writes one bounded framed
response under one aggregate two-second deadline and then closes. Exact submit grammar returns only
the temporary `submission_unavailable` error and performs no `Application` call. The package adds
only direct `kapsel`, Serde/JSON, and pinned Tokio `rt,time,net,io-util,sync` dependencies; no
socket, daemon, RPC, queue, protocol, storage, or identity framework is added.

A compile-time `test-harness` feature supplies only a caller-owned temporary socket, expected GID,
finite connection count, and fixed read-only `NOT_FOUND` fixture to the same executable. It is not a
product configuration or distributed service mode. The ordinary Slice 2 executable deliberately has
no startup/configuration composition until the separately gated fixed-root and installation slices
own it.

On 2026-08-15, native x86-64 Linux host `server` (Debian kernel `6.12.48+deb13-amd64`, Rust/Cargo
1.96.1) passed 17 package unit/socket tests and three default real-process allow, comparison-deny,
and saturation tests. One separately invoked ignored test also ran a client through the host's
existing supplementary `docker` group with `sg`: server expected EGID 1000, the caller's genuine
EGID was 992, and the denied client sent no frame and observed prompt close. No identity or group
was mutated. The exact-GID denials closed before body parsing or application access; the allow case
crossed the real executable and one framed status response. Saturation held eight admitted
connections, promptly closed the ninth with no read or response, then served only a new tenth after
one permit was released. The tests also passed exact frame limits, incomplete/trailing/idle and
staged prefix/body/write-half-close behavior under one aggregate deadline, complete and partial
write deadline behavior, authenticated hostile JSON and submit matrices with zero reads, every
status/rejection/receipt/error projection through framed sockets, ordinary/receipt response bounds,
a blocked application read concurrent with another expiring frame, real `Application` read
composition without Kubernetes access, and submit non-effect. macOS package, application, CLI, MCP,
demo-harness, formatting, link, and diff-hygiene gates passed separately; macOS is not claimed as
peer-credential evidence. Slice 2 adds approximately 422 non-test Rust lines and 1,071 test Rust
lines; combined with Slice 1's recorded approximately 698 non-test additions, cumulative KAP-0074
production additions are approximately 1,120, below the 3,000-line stop trigger.

Initial independent review found that saturation queued rather than closed the ninth connection,
synchronous reads could block reactor-owned deadlines, and several public-boundary matrices were
underproved. The sole writer corrected those findings with immediate non-waiting saturation denial,
a bounded blocking read boundary, production-owned aggregate deadline wrappers, complete framed
projection and hostile-input tests, and the distinct-EGID process gate. Three fresh correctness,
Linux-security, and test-quality rereviews found no remaining blocker, high, or medium issue. The
focused package/Clippy, application, CLI, MCP, demo-harness, formatting, links, diff-hygiene, and
complete `./scripts/ci-local.sh` gates passed after correction.

## Slice 3 authorization and exact submit records

The user explicitly authorized only Slice 3 on 2026-08-15: execution and disconnect behavior for the
already accepted socket and request grammar. This authorization permits the smallest correction in
this task needed to fix the exact Slice 3 submit responses and execution ownership. It does not
permit startup reconciliation, systemd or identity mutation, fixed-root configuration, installation,
credentials, provider or customer access, deployment, traffic, release, or Slices 4–6.

A grammar-valid submit that wins the sole non-waiting submission slot, matches the configured exact
grant, and installs one process-owned background `Application::execute` call returns exactly:

```text
{"status":"ACCEPTED"}
```

`ACCEPTED` means only that the background task owns both execution and the one in-flight slot until
`Application::execute` returns. It does not mean that a journal row is already visible, Kubernetes
accepted or performed a request, execution completed, a receiver result exists, or retry is safe.
The response may therefore precede durable submission visibility. Completion is retrieved only with
the existing status and receipt requests; there is no later unsolicited submit response. Response
failure, write timeout, or caller disconnect does not cancel the execution task or release its slot.
Process or runtime loss remains Slice 4.

A grammar-valid submission that cannot acquire the sole slot immediately returns exactly:

```text
{"status":"BUSY"}
```

`BUSY` waits for nothing, creates no queue, calls no `Application` method, and creates or changes no
lifecycle fact. A grammar-valid request that wins the slot but does not match the configured exact
grant, or for which execution ownership cannot be installed, returns exactly:

```text
{"status":"ERROR","error_class":"operation_failure"}
```

That error discloses no grant fact. Malformed request handling remains the Slice 2 `invalid_request`
record. An execution failure after `ACCEPTED` creates no transport-derived result; subsequent status
and receipt reads project only KAP-0038 durable facts.

Execution and read ownership are separate inside the one resident module. One execution-only
`Application` and one projection-only `Application` may open the same configured operation and sole
SQLite journal; this is two handles to one lifecycle store, not a second store or configuration.
Status and receipt keep the accepted standard-library mutex plus bounded `spawn_blocking` boundary.
Submission uses a non-waiting owned slot and non-waiting execution ownership; the spawned execution
task, not the socket handler, response, eight-connection permit, or caller, owns both until
`Application::execute` returns. No second execute call reaches KAP-0038 while that task is active.
After it returns, exact replay may execute the idempotent same-operation path, but after
`apply_started` KAP-0038 must observe and must not issue a second provider mutation attempt.

## Slice 3 implementation record

The accepted candidate replaces Slice 2's temporary submit error with exact process-local
`ACCEPTED`, immediate non-queued `BUSY`, and bounded `operation_failure`. One private resident state
holds a standard-mutex projection `Application`, an async-mutex execution `Application`, and one
submission permit. Admission uses only non-waiting owned acquisition. After the complete exact
request matches the configured grant, the spawned task captures the execution guard and permit
before the response is written and owns both until `Application::execute` returns. The socket
handler, caller, response, and eight-connection permit own neither, so disconnect or write failure
cannot cancel execution or admit another call. Status and receipt retain the bounded
`spawn_blocking` projection boundary. Both `Application` handles use the same configured operation,
authority, and sole journal; `kapseld` gains no SQLite, publication, provider, or second-store
logic.

Focused production-socket tests cross the real `Application` and deterministic Kubernetes fixture.
They prove exact acceptance, a provider-blocked competing `BUSY` with no second grant-match,
execute, or Kubernetes call, concurrent `IN_PROGRESS` status through the separate projection handle,
caller disconnect before response consumption, terminal target rejection, and same-operation replay
after one observed target GET, exactly one PATCH, and one receiver-observation GET. Replay reaches
the idempotent finalized path without another provider request. Existing framing, authentication,
deadline, saturation, hostile JSON, disclosure, status, receipt, and response-size matrices remain
green.

The compile-time Linux process harness remains synthetic and authority-free. Its atomic handshake
keeps one detached execution owner active through observed `IN_PROGRESS` and `BUSY`, then releases
it for terminal status; five-second bounds detect deadlock and do not define product execution time.
On the native x86-64 Linux host, all 23 package unit/socket tests passed. The current real-process
gate passed four default allow, comparison-deny, saturation, and execution/disconnect tests with one
ignored; the separately invoked distinct-effective-GID test passed through the host's existing
supplementary `docker` group. No identity, group, systemd, credential, Kubernetes, or provider state
was mutated.

Focused package and all-target/all-feature Clippy, application contract, operation-command, MCP,
deterministic process-recovery, formatting, links, diff-hygiene, cross-target Linux Clippy, and the
complete default gate passed. Initial independent review found and corrected a status-only harness
regression, scheduler-dependent synthetic execution timing, missing proof that `BUSY` calls no
application method, and stale architecture/testing status. Fresh correctness, Linux-security,
architecture, and test-quality review found no remaining blocker or high finding; its one remaining
medium architecture wording mismatch was corrected before final acceptance review. Slice 3 adds 220
non-test Rust lines under the existing accounting convention, bringing cumulative KAP-0074
production additions to approximately 1,340, below the 3,000-line stop trigger.

## Slice 4 authorization and startup contract

The user explicitly authorized only Slice 4 on 2026-08-16: startup reconciliation and real `kapseld`
process loss at the existing KAP-0038 mutation and receipt-publication seams. The existing Linux
compile-time `test-harness` may add one real-`Application` mode. It retains the test-owned socket
path, expected effective GID, and finite connection count and adds exactly one private fixture root,
one loopback Kubernetes fixture URL, and one fixed `A` or `B` receipt configuration. The exact test
operation, grant, authorization trust, and signing authority are compile-time fixed. These inputs
are private finite proof controls, not ordinary configuration or a caller interface.

One execution `Application` opens and calls `Application::reconcile` exactly once during each test
process startup. Only after that call succeeds does the projection `Application` open and the socket
bind. The reconciled handle then becomes the execution handle; no third application handle, store,
startup queue, or periodic retry exists. `Ok(None)` is a successful activation with no existing
operation. Every harness setup, application-open, reconciliation, projection-open, bind, accept, or
serve failure exits with code 4, writes nothing to standard output or standard error, leaves no
socket, and exposes no phase distinction. Slice 5 owns installed systemd diagnostics and fixed-root
startup behavior.

The real-process harness reuses only KAP-0038's compile-time-private `after_apply` and
`after_receipt_publish` seams and fixed owner-private markers. It must kill the real `kapseld`
process after one returned provider mutation, prove restart observes without another patch and does
not bind before startup reconciliation completes, kill the restarted process after frozen receipt
publication, then restart with fixed configuration `B`. Socket retrieval must return the exact
journal-frozen receipt bytes and digest created under configuration `A`; the original directory
contains that one unchanged receipt and configuration `B`'s directory remains empty. A separate
apply-kill/restart path must exhaust the unchanged production receiver-observation bound and expose
terminal `UNKNOWN` without another patch.

This authorization does not permit ordinary or fixed-root startup, `/etc/kapsel`, `/var/lib/kapsel`,
systemd units or lifecycle, identities, installation, stale production-socket handling, RBAC,
credentials, provider or customer access, deployment, traffic, artifact assembly, release, or Slices
5–6. It adds no public fault, configuration, administration, health, shutdown, lifecycle, store,
provider, protocol, or capability surface.

## Slice 4 implementation record

The compile-time candidate keeps ordinary startup uncomposed. One fixed real-`Application` harness
opens the execution handle inside the Tokio runtime, reconciles before projection open and bind,
then uses the accepted socket execution and projection owners. The finite server waits for an
already accepted execution before successful exit. Both test modes remove only the socket they bound
after finite serving; killed-process stale socket removal remains explicit parent-fixture work
rather than production stale-socket policy.

The Linux provider fixtures now require the exact target GET, conditional PATCH document, and
recovery GET transcript under finite process, socket, provider, and coordination deadlines. One
additional process case corrupts already published frozen bytes after the receipt-publication kill
and proves reconciliation failure exits 4 silently without binding. The other process cases prove
one PATCH, no bind during blocked recovery, exact configuration-A bytes and digest after
configuration-B restart, an empty B receipt directory, and terminal `UNKNOWN` after the unchanged
30-read schedule.

On Linux kernel `6.12.48+deb13-amd64`, Docker 29.5.2 ran the pinned x86-64 Rust 1.96.1 container.
The full `kapseld` feature gate passed 24 package unit/socket tests plus nine default real-process
tests; one distinct-effective-GID test was ignored there and passed separately against the host's
existing supplementary `docker` group. The first native process run exposed and corrected Kubernetes
client construction outside a Tokio runtime before any acceptance claim. This evidence uses no
systemd, identity mutation, real credential, Kubernetes cluster, or provider resource.

Slice 4 adds 131 and deletes 17 non-test Rust lines under the existing physical-line accounting
convention, a net 114 lines. Cumulative KAP-0074 production additions are therefore approximately
1,454 lines, below the 3,000-line stop trigger. Focused local gates, the complete default gate,
Linux Clippy, and the final native process gates passed after correction. Fresh correctness,
recovery/security, architecture, and test-quality rereviews found no blocker, high, or medium
finding. Final independent acceptance review found no blocker, high, or medium finding, and the
focused package/Clippy plus complete local default gate passed again before Slice 4 acceptance.

## Slice 5 authorization and exact installation contract

The user explicitly authorized repository-only Slice 5 contract, production, test, static service
asset, and documentation changes on 2026-08-16. The accepted identity choice keeps
`kapsel-preview-caller` operator-owned and pre-existing: Kapsel adds and later removes only its
supplementary `kapsel-preview-callers` membership, while the caller's own supervisor must set that
group as the process effective GID. The accepted Kubernetes subject is the non-secret
`ServiceAccount/demo/kapsel-preview`. Privileged qualification is deferred: this authorization
permits unprivileged local and Linux gates only and performs no VM creation, account or group
change, write beneath system paths, `systemd-sysusers`, `systemctl`, Docker/kind, Kubernetes,
credential, namespace, RBAC, or resource mutation.

KAP-0054 owns the complete corrected inventory. Slice 5 uses exactly these direct assets and no
installer or generated variant:

| Repository input                              | Direct-install destination                      |
| --------------------------------------------- | ----------------------------------------------- |
| feature-free `kapseld` executable             | `/usr/libexec/kapsel/kapseld`                   |
| `crates/kapseld/deploy/kapseld.service`       | `/usr/lib/systemd/system/kapseld.service`       |
| `crates/kapseld/deploy/kapseld.conf`          | `/usr/lib/sysusers.d/kapseld.conf`              |
| `crates/kapseld/deploy/kapseld-rbac.yaml`     | `/usr/share/kapsel/kapseld-rbac.yaml`           |
| operator-provisioned existing JSON and inputs | fixed `/etc/kapsel` and `/var/lib/kapsel` names |

Ordinary `kapseld` accepts only the exact ordered
`--operator-config /etc/kapsel/operator.json --socket /run/kapsel/kapseld.sock` arguments, no
environment configuration, and no finite connection count. It adopts the existing operator JSON keys
and bounds through one root `kapsel` parser/composition owner shared with CLI and MCP; copying the
grammar into `kapseld` is forbidden. The path values are exactly the fixed KAP-0054 names. Expected
peer GID is the daemon's effective GID. One execution application opens and reconciles once; only
after success does one projection application open and the secured socket enter an indefinite accept
loop.

Startup retains descriptor-relative configuration, state, and runtime roots. It requires the exact
owners and modes fixed by KAP-0054, reads authority bytes from validated regular single-link opened
files, permits the journal and worker lock to be absent only before their normal first creation, and
constrains resident frozen receipt paths to the fixed receipt directory. The caller cannot write the
state root; journal opening is anchored beneath the retained state root and verifies named identity
around SQLite's pathname open. A custom SQLite VFS and protection against trusted host root, kernel,
or compromised service identity are out of scope. This interpretation preserves stable consumed
operator/receipt inodes without importing historical sandbox isolation.

Before bind, an absent socket is allowed. An existing leaf is removable only when no active listener
answers and its descriptor-relative identity is an exact single-link socket owned by service UID and
caller-group GID with mode `0660`; the daemon leaves every other leaf unchanged and startup fails.
After that process exit, systemd removes the service-owned runtime directory and any refused leaf;
installed behavior does not preserve it across manager cleanup. After bind, the process sets and
verifies exact socket type, owner, group, and mode before admission. All ordinary socket cleanup
remains systemd `RuntimeDirectory` behavior rather than daemon shutdown logic.

The one unit is `Type=exec`, has the exact fixed user/group/directory/mode/restart values,
`UMask=0077`, null standard streams, no start-rate limit, the exact argv above, and
`WantedBy=multi-user.target`. `Type=exec` establishes only successful process execution, not
post-reconciliation readiness. Successful authenticated socket use plus systemd state remains the
health boundary. Any ordinary startup or accept-loop failure exits 4 without process output. The
compile-time Slice 4 harness retains its already accepted silent finite behavior. Installed
allowlisted diagnostics are only systemd `ActiveState`, `SubState`, `Result`, `ExecMainCode`,
`ExecMainStatus`, and `NRestarts`; they state no provider, receiver, or production claim.

The sysusers file creates only install-allocated `kapsel`, its private same-name group, and
`kapsel-preview-callers`. It does not create or own the external caller. The RBAC manifest contains
only the fixed ServiceAccount with token automount disabled, one namespaced Role granting exact
`apps/deployments` `get` and `patch` with `resourceNames: ["agent-api"]`, and one exact RoleBinding.
It creates no credential, token Secret, Namespace, Deployment, workload, ClusterRole, wildcard, or
field policy. Static validation can prove these bytes; credential use and live allow/deny behavior
require the deferred disposable gate.

Strict vertical TDD proceeds in this order: one fixed-root denial/consumption assertion and minimum
implementation at a time; ordinary reconciliation-before-bind; stale-socket identity; static
unit/sysusers/RBAC drift; then existing Slice 1–4 regressions. No privileged clean-install claim can
be recorded before target-specific authorization and a fresh x86-64 Debian 12 systemd environment.
Slice 5 may build and copy direct inputs from a clean repository revision, but it owns no archive,
checksum, SBOM, provenance, reproducibility, installer, source-independent install, external
request, or acceptance review for an exact artifact. Those remain Slice 6.

The Slice 5 accounting baseline is approximately 1,454 substantially added non-test production
lines. Reaching 3,000 cumulative lines, adding another runtime surface/store, or requiring a custom
SQLite VFS stops implementation and reopens KAP-0054.

### Slice 5 source-candidate record

The repository candidate now shares the existing operator parser and bounded Kubernetes-client
composition through `Application`, constrains fixed resident state and frozen receipt paths, retains
validated fixed-root authority bytes, reconciles before projection open and bind, serves only the
exact ordinary argv, and implements exact socket identity/stale removal. The sole unit, sysusers
record, and non-secret RBAC manifest are frozen by one static byte test. No installer, second
grammar, store, lifecycle owner, runtime dependency, or Slice 6 artifact surface was added.

The complete local default gate passed with 97 root unit tests, 14 application contract tests, all
CLI/MCP and repository tests, Clippy, rustdoc, formatting, Markdown, and diff hygiene. On the native
Linux host kernel, the pinned Rust container passed 29 package unit/socket/startup tests, the static
asset test, and 14 default real-process tests with one ignored; the distinct-effective-GID case then
passed separately using read-only host passwd/group inputs and the host's existing supplementary
`docker` group. Native all-target/all-feature Clippy also passed. These are unprivileged source and
process results only.

Slice 5 adds 613 and deletes 157 non-test Rust physical lines under the existing convention, a net
456 lines; cumulative KAP-0074 non-test Rust additions are approximately 1,910. The 53 static
service/RBAC lines bring the broader production-input estimate to approximately 1,963, still below
the 3,000-line stop trigger. Fresh independent source, security, systemd-asset, architecture, and
test review inspected the actual changed files and found no blocker, high, or medium issue. No clean
systemd, separate-identity, credential, Kubernetes/RBAC, uninstall, or source-independent
installation gate was run because those mutations remain unauthorized. Slice 5 therefore remained
unaccepted at that source-only checkpoint.

### Slice 5 clean-install qualification record

The user separately authorized the complete disposable privileged gate on 2026-08-26. Exact source
snapshot SHA-256 `469db9a7b3827265f2334e48ce95f8d424b93b174da6dfa4d941e3ed27e83fba` was built with
Rust 1.96.1 on x86-64 Debian 12. A fresh KVM Debian 12 VM with systemd 252 directly installed
feature-free `kapseld` and the three static repository assets. The gate used kind 0.32.0, kubectl
1.33.13, and exact Kubernetes node image `v1.33.12` at the repository-owned digest. It created only
one disposable namespace, exact target and decoy Deployments, the static preview RBAC objects, and
one 30-minute TokenRequest credential; all were removed before VM destruction.

The separate-identity gate proved clean sysusers creation, locked non-login service ownership,
caller denial of every fixed authority/state root, denial when supplementary membership did not
match the effective GID, and allowed authenticated socket use only with the exact effective caller
group. Systemd proof covered enabled boot, explicit start/restart, `Restart=no`, zero automatic
restarts after exit 4 and SIGKILL, allowlisted secret-free diagnostics, descriptor failure, active
exact-socket refusal, inactive exact stale-socket replacement, nonexact-leaf refusal, connection
closure, and runtime-directory cleanup. The gate exposed and corrected one owner-document
contradiction: the daemon refuses a nonexact leaf without unlinking it, but systemd then removes the
whole service-owned runtime directory after the failed activation; installed behavior cannot promise
that the leaf survives manager cleanup.

The live operation changed only container `api` to the exact pre-authorized immutable digest,
preserved the Deployment UID and sidecar, advanced generation once, survived real process loss after
the mutation, reconciled to `SUCCEEDED`, returned exact receipt bytes with frozen SHA-256
`bc5beeabf2e4be041d019139b7fb3071c1a758528ff7ed429b692d55c9c9ac06`, passed offline inspection under
separate trust, and replayed plus restarted without another generation change or changed receipt
digest. The actual short-lived credential passed named `get`/`patch` only for
`Deployment/demo/agent-api` and denied the decoy, list/watch, other verbs/resources/namespaces, and
cluster scope. Ordered uninstall closed an idle connection, removed caller membership, revoked the
RoleBinding before proving the old credential denied both allowed verbs, deleted ServiceAccount and
Role, removed only the four installed assets, retained exact authority/journal/receipt bytes and
metadata unchanged, deleted the owned cluster, destroyed the VM, and retained only bounded
secret-free evidence at `/tmp/kapsel-slice5-evidence.json` outside the repository. That
schema-backed attestation names every asserted category, the snapshot method, base revision,
qualified binary, current reviewed source manifest, environment and tool versions, exact RBAC
allow/deny matrix, lifecycle and socket cases, operation/receipt/replay facts, uninstall ordering,
retained-data comparison, cleanup, and non-claims. Its SHA-256 is
`c815500145bbb56c51d85e699a8fcddd3895eba81398a067d7543b095aaeb16a`.

After owner/status correction, the complete explicit live-kind gate also passed the healthy,
`ProgressDeadlineExceeded`, and deleted-after-patch `UNKNOWN` cases against the exact pinned node
image and deleted its owned cluster. This gate proves the Slice 5 direct source-install path, not
Slice 6 artifact identity, source-independent installation, external use, retained demand, or
production safety. Fresh final architecture, qualification, and security reviews inspected the
corrected complete diff and bounded evidence; each found no blocker, high, or medium issue and
accepted Slice 5.

## Acceptance

- Runtime topology is five boxes or fewer and dependency direction remains `kapseld -> kapsel`.
- Only one new package, executable, authenticated interface, systemd unit, sysusers file, and
  non-secret RBAC manifest exist; no second state store exists.
- The exact three-request vocabulary and all KAP-0054 bounds are black-box tested.
- Separate identities prove the caller cannot read or select operator authority, state, or receipt
  paths and cannot connect without the exact local authorization.
- Caller loss, service loss, and restart preserve stable operation identity, one provider attempt
  after `apply_started`, observation-only recovery, explicit `UNKNOWN`, and frozen receipt bytes.
- Read-only status and receipt retrieval neither call Kubernetes nor advance lifecycle.
- Exact receipt bytes round-trip through the interface and match the journal-frozen digest without
  exposing the private path.
- Systemd service state and diagnostics are bounded, secret-free, and make no
  provider/result/production claim; the socket has no separate health request.
- A competent operator completes clean install, operation, reconnect, inspection, explicit restart,
  caller/Kubernetes authority revocation, and uninstall without source modification, standing
  maintainer access, Python, or a required shell script.
- Existing `application_contract`, CLI, MCP, receipt, process-kill, default, and explicit live-kind
  gates remain green where their prerequisites apply.
- Fresh independent security and architecture review has no unresolved blocker, high, or medium
  finding.

## Unsupported behavior

No production use, host-loss continuity, backup, HA, fleet management, concurrency queue, periodic
controller, broad upgrade/rollback matrix, online key or identity rotation, enterprise identity,
public HTTP, remote client, container package, SDK, plugin, second capability, arbitrary image
change, arbitrary provider input, hosted authority, managed coordination, or compatibility promise.
The exact preview build may be replaced only by a new decision and evidence packet.

## Stop conditions

Stop and reopen KAP-0054 if work crosses the line/time cap, needs another runtime surface or store,
cannot serve status during an in-process submission without a queue/controller, needs Python or
shell runtime semantics, duplicates private SQLite/publication behavior, changes KAP-0038 semantics,
requires sandbox reuse, or reveals that the representative workflow needs more than one pre-armed
exact operation.

## Residual risk

Passing this packet would prove only one clean non-production installation. It would not prove
retained use, production safety, operational preference over GitOps, broader workflow fit, support
economics, or demand for another capability.
