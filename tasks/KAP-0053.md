<!-- work-id: KAP-0053 -->

_Track: hosted technical proof · Area: sandbox deployment · Created: 2026-07-20_

# Prove The Isolated Live Sandbox

Status: superseded deployment route. KAP-0069 selected a serialized controller-host reshape,
KAP-0070 later closed through fallback, and KAP-0073 retires the deployable sandbox. Contract Lock
(Gate 0), corrected Authority Composition Proof (Gate 1), the provider-neutral
[KAP-0055](KAP-0055.md) runner handoff at exact revision `b36af376066b002c33ef584187a726e49634337f`,
and accepted offline tranches retain only KAP-0069's individual classifications. The
Kubernetes-hosted system/controller/stager topology, remaining render, GKE fixture, and this
packet's Infrastructure Enforcement and live gates may not resume. They are historical evidence, not
deployment input or authorization. The privacy owner now treats the sandbox as a synthetic,
non-consequential prototype: Kapsel-owned records keep their explicit lifecycle, while minimum
provider management and audit records follow documented provider storage semantics under strict
field, routing, and non-disclosure limits. GKE Sandbox was this packet's first candidate; it was
never selected or authorized. Independent final review of planning revision
`fe4ca1edb68dd527bf4c6de185beecd6f540bb2d` found no blockers. Authority Composition Proof was
separately authorized and implemented in revision `eb62a92e30cfa77863de2d3cee305b4224db2a2c`; later
review withdrew its acceptance because its shared gateway-journal volume contradicted the deployment
contract. Revision `6949ebfa35fae63cd20ca4f24e9e116004d1fdbe` corrected that topology, but a
subsequent review found that its runner could not receive the complete operator-owned `Application`
composition and used an identity inconsistent with target authorization, so Authority Composition
acceptance was withdrawn again. The runner composition was corrected in revision
`bd67be9b469672b895a6214322b4dc7ff942da33`; focused and full deterministic gates passed, and its
clean local `linux/arm64` image was locked as
`sha256:4d85515113eccf5cb56618fd5b406632111ac429a25352e385942c40733d3480`. Fresh independent review
of evidence revision `e757ce0adbc79d2f36209155149f03506f93e69b` recomputed the fixture digest, reran
the focused gate, reproduced the exact clean image build, and found no blocker. Corrected Authority
Composition was accepted as offline evidence. Infrastructure Enforcement Proof (Gate 2) and every
live phase were never authorized, and this route is now retired. No provider was selected.

> **Historical record only.** All dependencies, architecture triggers, commands, GKE/provider
> statements, unfinished steps, planned result, work, acceptance criteria, and Gate 2–4 text below
> preserve what this packet proposed or evidenced at its cited revisions. None is current guidance,
> deployment input, a resumable gate, or authorization. Do not run the old Gate 1/Gate 2 commands,
> refresh their fixtures, continue the GKE composition, request provider access, or perform a live
> action. Follow only [KAP-0073](KAP-0073.md) for sandbox removal.

Historical problem: deterministic service tests could not establish that real infrastructure
isolated runs, contained authority, survived runner failure, bounded capacity, and cleaned owned
resources. This packet therefore would not have allowed the public site to connect before proof from
an exact revision. It never produced that proof; KAP-0070 later closed through fallback.

Direct owners: the sandbox API, deployment, threat, privacy, and testing contracts produced by
[KAP-0051](KAP-0051.md), the service package from [KAP-0052](KAP-0052.md), and umbrella
[KAP-0050](KAP-0050.md).

Historical dependencies were [KAP-0052](KAP-0052.md), the provider-neutral
[KAP-0055 runner handoff](KAP-0055.md), and an accepted
[KAP-0056 architecture classification](KAP-0056.md) with no unresolved pre-GKE blocker. This
packet's planned completion would have allowed the independent `kapsel.cloud` and Grafik consumer
packets to connect, but completion never occurred. KAP-0073 cancels that live connection route.

## Planning record

This task retains the historical finite experiment, reproduction lock, decision criteria, provider
screens, measurements, gates, rejection rules, and missing evidence. The historical
[deployment owner](../docs/SANDBOX_DEPLOYMENT.md#retired-route) records KAP-0070's retired
serialized native-host route and explicitly supersedes this controller/stager composition. The
historical Kubernetes/provider statements below are not current deployment input or authority. The
screen found provider-managed records on GKE, AKS, and EKS. The privacy owner now requires exclusion
from Kapsel-controlled stores and no Kapsel query or replay after 24 hours rather than an
unverifiable provider-wide inaccessibility or physical-erasure claim, while continuing to prohibit
visitor, request, receipt, journal, workload, application, and secret payloads in provider logs. At
that historical planning point, GKE Sandbox reopened first because its other decision surfaces had
the narrowest evidence; AKS and ordinary EKS remained alternatives, EKS on Fargate remained rejected
on NetworkPolicy, and self-operated runtimes remained held out. KAP-0069 later superseded all of
those provider routes for this packet.

Planning found concrete execution prerequisites at the accepted base: `kapsel-sandbox` was a
library, not a native listener binary; its accepted deterministic service used local SQLite and
receipt paths plus an in-process private digest key; and `Application` composition received a raw
Ed25519 receipt seed. Authority Composition added only the fixed listener/operator control,
deployment fixture, evidence harness, durable volume/store and fenced backup/restore composition,
exact operator-owned patch admission, and raw-seed known-answer/stop fixture needed for this
experiment. Managed custody compatibility, IAM denial, audit, trust, outage, rotation, and
continuity remain live Infrastructure Enforcement and Failure Recovery Proof (Gates 2 and 3)
evidence. Non-export signing that cannot satisfy the existing interface is a stop-and-review
condition, not permission to redesign receipt semantics.

No account, credential, secret, provider resource, spend, DNS name, endpoint, deployment, or public
request was created or used in this planning wave. Consequently there is no current evidence of
isolation, custody, cleanup, restart, rollback, stop, saturation, bounded cost, or live
reproducibility, and the packet does not approve public traffic.

## Historical Authority Composition architecture triggers

The completed Authority Composition Proof (Gate 1) used the following private, conditional rules.
They are preserved as review history, not current refactor permission:

- A private target-ownership module was permitted only when Authority Composition work concretely
  touched scattered policy inventory, provisioning facts, UID or cross-run ownership, cleanup state,
  or absence evidence. It had to stay behind the existing `Service` seam and not absorb admission,
  scheduling, `Application` lifecycle, or receiver classification.
- A private HTTP `v1` adapter was permitted only alongside the native listener when exact
  request/response translation created concrete pressure. It had to stay behind `handle_http` and
  own transport grammar only, not orchestration or a public framework or interface.
- KAP-0038 receipt-wire extraction was explicitly deferred and was not part of KAP-0053 unless a
  concrete defect required it or an existing documented extraction trigger passed.

None was a separate packet or workstream. Any refactor used under these historical triggers had to
preserve behavior and be validated through the existing interfaces.

## Historical Authority Composition correction evidence

The accepted correction retained one native `kapsel-sandbox` HTTP/1.1 process which incrementally
bounded raw request lines, request heads, headers, bodies, open connections, in-flight requests, and
receive time before delegating exact route behavior to `Service::handle_http`. Its 30-second bound
was only the connection's response wait and did not cancel continuing service work. The same binary
exposed fixed local `stop` and `clear-stop` commands, not HTTP routes. Those commands opened only
the existing private admission database, required one compatible singleton row and read-back, and
did not require receipt storage, tombstone-key availability, retention, or full `Service::open`.
Black-box restart tests proved stopped new admission, retained replay, clear, and transport
rejection without admission.

At the cited revision, `deploy/sandbox/` separated one API-owned system-state volume from one owner-
private gateway-state volume per active run. Revision `bd67be9b469672b895a6214322b4dc7ff942da33`
defined one per-run identity across the journal mount, target-namespace RoleBinding, and exact
Deployment patch admission. Its canonical runner Pod had exactly one writable PVC plus explicit
read-only projected channels for bounded Kubernetes controller credentials, signed grant and trust,
receipt-signing inputs, operator-composition paths, and receipt-handoff authority. A complete
normalized-object comparison rejected every undeclared Pod field. The fixture also retained the
three contract-owned terminal journal deletion paths and the multi-volume backup-generation
protocol. These were offline topology and rule proofs only; the native runner and private handoff
remain accepted separately in KAP-0055, while the old route never proved real Kubernetes admission,
identity denial, storage fencing, custody, snapshot consistency, restore, or retention.

The image IDs `sha256:08ced4f2100fd4eb956ec794fed6a46207b873870dab4738b542bc1b873a2675` and
`sha256:5e0c30bb4a73b3e3ca460c11f035c5a8d9f840d8341af4b89834629fbc49e843` belong only to their
superseded execution revisions. The reproduction lock preserves both historical records and records
runner-composition revision `bd67be9b469672b895a6214322b4dc7ff942da33`, local `linux/arm64` image ID
`sha256:4d85515113eccf5cb56618fd5b406632111ac429a25352e385942c40733d3480`, and independently
reviewed evidence revision `e757ce0adbc79d2f36209155149f03506f93e69b`. The historical raw-seed lane
was the RFC 8032 32-byte Ed25519 seed/public-key/signature known answer plus a receipt produced
through the existing `Application` and production inspector under separate trust. Its then-focused
command was `cargo make test-sandbox-gate1`; that command is preserved only as revision evidence and
is not current runnable guidance.

The focused Authority Composition lane and `./scripts/ci-local.sh` passed on the reviewed working
tree. Fresh independent review first rejected incomplete exact-runner admission, contradictory
backup ordering, and non-literal listener limits; follow-up corrections added exact workload
equality/denial cases, checkpoint-before-detach seam preservation, and literal
128/64/5-second/30-second assertions. A second fresh review found no remaining implementation
blocker, and evidence review reproduced the then-locked image. Later topology review found that the
canonical Pod still lacked the authority channels needed to construct `OperatorConfiguration`, used
conflicting runner identities, permitted only one total volume, and did not reject every undeclared
command/environment/projection field. That finding withdrew the prior acceptance. The
runner-composition correction passes the focused and full gates and passed independent
implementation review. Final evidence review checked clean commit
`e757ce0adbc79d2f36209155149f03506f93e69b`, recomputed fixture digest
`054dfc6c841e7635b64e82b68cf3ae1fe39c8b955cc7599bbe500d16c6fb568c`, reran the focused gate, rebuilt
execution revision `bd67be9b469672b895a6214322b4dc7ff942da33` from a clean detached checkout,
reproduced the exact `linux/arm64` image ID, and found no blocker. Corrected Authority Composition
is accepted; all Infrastructure Enforcement and Failure Recovery properties remain unproved.

## Historical Infrastructure Enforcement reopening evidence

A separately authorized 2026-07-23 account-readiness inspection used no stored account identifier
and created no provider resource. Billing is active. Compute Engine, GKE, Artifact Registry, Secret
Manager, and IAM Credentials APIs were enabled with operator approval; this project-configuration
mutation may create Google-managed service identities and is not deployment authorization.

The account-visible regional screen found Regular-channel GKE `1.35.6-gke.1127000` in
`europe-north1` and `1.35.6-gke.1049000` in both `europe-west1` and `us-central1`; all three exposed
Stable `1.34.8-gke.1278000`. These are dated availability facts that would have required recheck
under the retired route. The initially priced three-node `e2-standard-8` shape was rejected before
provisioning because it would have consumed the screened E2 CPU allowance while Standard GKE Sandbox
also required a separate non-sandbox node pool with at least one node. The then-current candidate
proposed three one-per-zone `n2-standard-4` gVisor nodes plus one `e2-standard-2` system node in
`europe-north1-a`; live scheduling, quota, and price were never proved.

Official documentation retrieved during the historical screen stated that Standard
[GKE Sandbox](https://cloud.google.com/kubernetes-engine/docs/concepts/sandbox-pods) used gVisor on
sandbox-enabled Container-Optimized OS with containerd nodes, requires at least one non-sandbox node
pool, supports the Compute Engine Persistent Disk CSI driver, and recommends an explicit
metadata-address NetworkPolicy when Workload Identity Federation is enabled. GKE
[Dataplane V2](https://cloud.google.com/kubernetes-engine/docs/concepts/dataplane-v2) uses
Cilium/eBPF and keeps Kubernetes NetworkPolicy enforcement on. GKE
[network isolation](https://cloud.google.com/kubernetes-engine/docs/how-to/latest/network-isolation)
supports private nodes and an IAM-authorized DNS endpoint while disabling both control-plane IP
endpoints. The candidate uses that route but still requires an exact private operator IAM and
Kubernetes RBAC binding. Secret Manager accepts
[opaque file bytes](https://cloud.google.com/secret-manager/docs/add-secret-version), so an exact
32-byte seed remains plausible; live byte equality, narrow export, IAM denial, audit, outage,
rotation, and deletion guards remain unproved. GKE's public
[cluster fee](https://cloud.google.com/kubernetes-engine/pricing) was `$0.10` per cluster-hour when
retrieved; the complete regional list-price ledger is still incomplete.

Pre-authorization image work was committed at exact historical revision
`86680191767c550a84a9fca872a928198ec8112e`. A clean detached `linux/amd64` build reproduced local
image ID `sha256:7b8c2d2a2701ad5225e4536ccf80a42cf0f0d93359ae145628f898265f036680` at 26,273,210
bytes. Cosign 3.1.2 verified the pinned Distroless runtime's keyless signature, and Trivy 0.72.0
with its 2026-07-23 database reported no `HIGH` or `CRITICAL` package vulnerability. The historical
file `gate2-image-candidate.json` at that exact revision recorded the inputs and non-claims; it is
named as Git history rather than linked to the working tree because KAP-0070 Gate 0 deletes it. This
evidence replaced neither the historical Gate 1 image evidence nor the missing registry
digest/runtime proof.

The prior reviewed versions of `gate2-gke-fixture.json` and `gate2-gke-storage-class.json` at exact
historical revision `54e9907299041f69ead69631e0d00b77f1b1de64` represented the non-executed
candidate with a one-day configurable Logging bucket. The files are named as Git history rather than
linked to the working tree because KAP-0070 Gate 0 deletes them. That prior fixture fixed GKE
Standard regional `europe-north1`, Regular `1.35.6-gke.1127000`, COS/containerd, Dataplane V2,
private nodes, DNS-based operator access with IP control-plane endpoints disabled, the four-node
split, regional balanced PD with `ReadWriteOncePod`, three separate key identities, full owned
inventory, partial-failure teardown, a 24-hour window, and a `$100` gross ceiling. Independent
review hardened revision `54e9907299041f69ead69631e0d00b77f1b1de64`, fixture digest
`sha256:5642204b21e10ab54441a03af6bca587743760afba2fcb4329821e221c2f6c5d`, through exact project
scope, node and identity commands, per-resource absence checks, teardown coverage, and negative
mutations. The then-in-progress replacement deliberately cleared that reviewed revision and digest
pending execution-composition changes and independent review; it was never accepted.

The operator selected `europe-north1` for completing a GKE decision packet and authorized a
non-executed infrastructure fixture, not spend or provisioning. Cloud Logging's immutable
`_Required` records and the minimum configurable audit evidence may follow documented provider
storage semantics because this candidate carries only synthetic, non-consequential data. The retired
Infrastructure Enforcement plan would have inspected actual fields and rejected any visitor, request
body, receipt, journal, workload output, application diagnostic, or secret payload. It also would
have excluded configurable records from long-lived default buckets, routed at most one copy with the
shortest practical logical retention, and made them unavailable to Kapsel query or replay after 24
hours.

The account screen and public catalog put the proposed four-node GKE shape's 24-hour compute,
cluster, boot-disk, state-disk, one snapshot generation, 1 GiB registry, and three-secret subtotal
at `$21.13` before telemetry, network, object storage, taxes, failed cleanup, and contingency. That
subtotal was evidence that the proposed `$100` maximum was plausible, not permission to incur cost.
At that time GKE Sandbox was reopened only for an exact fixture and final authorization packet. That
reopening is superseded and cannot resume. No cluster, registry, secret, image push, storage object,
endpoint, DNS change, or public traffic was created.

Post-fixture review found that the proposed one-day Cloud Logging bucket was unnecessary and gave a
misleading physical-deletion impression. Google's then-current
[log-routing documentation](https://cloud.google.com/logging/docs/routing/overview) permitted the
project `_Default` sink to exclude configurable Data Access and Policy Denied records and route one
bounded copy to Pub/Sub. The replacement fixture used one topic and one private-review subscription
at Pub/Sub's documented 600-second minimum message-retention duration, did not retain acknowledged
messages, required a unique Logging sink writer with only topic-level `roles/pubsub.publisher`,
restored both the `_Default` exclusion and topic policy from optimistic baselines, and deleted the
subscription and topic during teardown. It retained the 24-hour maximum as an absence/query/replay
ceiling and made no physical-erasure claim. The official
[Logging Pub/Sub destination](https://cloud.google.com/logging/docs/export/configure_export_v2#pubsub),
[Pub/Sub retention](https://cloud.google.com/pubsub/docs/subscription-properties#message-retention-duration),
and then-current `gcloud` command references were locked in the fixture. The reviewed execution
revision, fixture digest, sink-writer identity and policy binding remained null blockers; no
provider command was run.

Provisioning was also premature while the accepted provider-neutral runner lacked the deployed
scheduler, cleanup-controller, and periodic-retention composition named by the accepted Gate 1
fixture. Offline execution-readiness review had found a direct topology contradiction:
`Service::execute_application` and `Service::reconcile_application` mutate the sandbox admission
SQLite database, but the accepted Gate 1 runner has exactly one writable per-run gateway-journal
volume and no system-state mount. The owner resolved this with one narrow private handoff. The
runner receives the immutable server-owned request through operator composition and verifies that it
matches the separately trusted grant. It sends a per-run-authenticated, restart-idempotent
invocation marker before its first `Application` call, then sends only one matching terminal
`OperationReport` after `Application::execute` or `Application::reconcile` returns. `not_attempted`
carries no receipt; `finalized` carries the exact frozen receipt digest and bytes. The system
service remains the sole admission-database and receipt-store writer, acknowledges only durable
commits, and rejects changed replay. The handoff accepts no public request, provider input,
lifecycle choice, cleanup transition, or result override. Sharing the system-state volume remains
forbidden.

The finite provider-neutral [KAP-0055](KAP-0055.md) slice is accepted at exact implementation
revision `b36af376066b002c33ef584187a726e49634337f` after focused/full gates and fresh independent
review found no blocker. It owns only the invocation/report messages, authenticated local transport,
lease replay and stale-runner denial, same-operation reconciliation, exact receipt handoff, and
receipt-free `not_attempted` proof while system SQLite remains private.

At that historical point KAP-0053 resumed only its GKE execution-composition planning: replace the
stale bucket route; compose the existing service roles without widening KAP-0038 or adding a generic
provider abstraction; lock the Kubernetes token audience, key-staging path, IAM/RBAC and rendered
manifests; rebuild and scan the exact `linux/amd64` image; then replace and independently review the
authorization fixture. No reviewed execution revision resulted, and KAP-0069 later prohibited
continuing those steps or supplying private account, cleanup, time, or spend bindings.

The first service-role increment exposed `Service::sweep_retention` as a dedicated native
`retention` process. It opened the same owner-private system database, receipt store, and tombstone
key as the API and private handoff roles, performed the existing startup sweep, and repeated it
every 60 seconds without accepting an origin, listener, public route, provider input, or runner
state. The runner could not mount system state.

Before the scheduler increment, implementation review resolved one sequencing ambiguity in the
deployment owner: transactional scheduler dispatch reserves active capacity, establishes the
deadline, and appoints the private lease before Kubernetes provisioning; complete policy
verification still precedes runner creation and Kapsel invocation. This is the existing `Service`
order and keeps partially provisioned resources inside the eight-active bound. It does not promote
dispatch, setup, or lease state into a KAP-0038 lifecycle or receiver fact.

The scheduler increment began by replacing the accepted service package's symbolic ten-object
`sandbox-policy-v1` inventory with candidate `sandbox-policy-v2`: eleven complete fixed Kubernetes
objects rendered inside `kapsel-sandbox`, including the previously missing target-workload service
account. Admission binds exact per-object canonical digests and a locked aggregate render digest.
The renderer pins the candidate runtime and Pod Security minor, uses a distinct immutable baseline
image so the healthy scenario still exercises a transition, states every contract-owned count
including explicit zero Secret/PVC/Job ceilings, and admits only enumerated Kubernetes-assigned
identity, status, and fixed-default fields during later normalized observation. This did not create
or observe a Kubernetes object; a concrete scheduler under that retired route would have had to
consume the same renderer and recompute those normalized digests before runner creation.

The native `scheduler` candidate consumed that renderer through the concrete `kube::Client`. On each
bounded pass it visits every active run in durable admission order before one new FIFO dispatch,
reuses a current lease until its renewal boundary, refuses an unexpired foreign lease, creates
missing fixed objects or observes existing ones, admits only enumerated server defaults, and commits
policy verification only after exact UID, cleanup owner, content, revision, and inventory digest
agreement. Every remote step is inside one 20-second lease-bounded pass, refreshes lease time before
mutation and final commit, and resolves an exact create conflict with one observed equality check.
Runs that have reached invocation, terminal projection, or cleanup are skipped before lease or
Kubernetes mutation; a foreign lease does not starve later active recovery and blocks fresh
dispatch. Deadline handling appends only the existing sandbox deadline event. The role then derives
but did not expose the accepted private handoff assignment. The scheduler used one concrete remote
`scheduler-state-v1` client over the authenticated controller transport; the system-side listener
alone opens `Service`, supplies operation time and the private handoff endpoint after exact
scheduler TokenReview, and retains SQLite, receipt, and tombstone-key ownership. The scheduler CLI
rejects those system-state inputs and requires separate render-owned state TLS, state-token, and
Kubernetes CA/token paths. Native-process and focused TLS tests cross the complete state/Kubernetes
sequence, deny credential-lane collision and TokenReview outage, and recover reserve, provisioning,
policy-commit, and assignment response loss without a public result, receipt, or cleanup change. It
remains intentionally non-deployable: no runner Pod, lease-specific Secret/ConfigMap, journal claim,
key staging, runner-resource UID record, rendered projected tokens, IAM/RBAC, or NetworkPolicy
binding exists. The incomplete Gate 2 wrapper therefore still omits the scheduler rather than
presenting policy-only setup as runnable execution composition.

The following native `cleanup` candidate consumed only durable active/eligible ownership evidence.
It scans the fixed runner namespace by exact cleanup-owner label, refuses unknown labeled UIDs,
requests deletion only with the recorded UID precondition, deletes external objects before the owned
namespace, and re-observes every recorded name/UID/owner before the existing completion transition
releases capacity. Delete acceptance, finalizers, timeout, API failure, and identity mismatch remain
retryable cleanup facts. One coalesced `cleanup.failed` event preserves receiver meaning; private
restart-safe start/escalation fields permit one escalation only after 15 minutes. The incomplete
wrapper still omits this role until its fixed state adapter, projected tokens, RBAC, NetworkPolicy,
and complete runner-resource UID recording are bound.

Execution-topology review then found that adding the two controllers as sidecars would be unsafe,
not merely incomplete. The system SQLite and `ReadWriteOncePod` claim require one Pod owner, while a
Pod has one Kubernetes service account. A union API/scheduler/cleanup identity would violate the
separate controller and cleanup key-denial contracts; separate controller Pods cannot mount the
claim. The then-canonical deployment owner locked one singleton API/handoff/retention state Pod plus
separate scheduler and cleanup Pods. Two fixed private state protocols exposed only the existing
bounded scheduler and cleanup transitions, authenticate exact role-specific bound tokens through
`TokenReview`, and remain transport adapters rather than a generic backend. System and controller
identities receive no Secret Manager IAM; separate stagers own tombstone, grant, and receipt inputs.
The then-current unreviewed fixture's `sandbox-api` GSA mapping and tombstone-secret IAM grant were
therefore superseded blockers rather than candidate authority and would have required removal if
that topology had been rendered. The role payload tranche was complete: revision `2d3661d`
implemented and tested the fixed `scheduler-state-v1` codec and exact `Service` transitions, and
revision `6aa30eb` separately implements and tests `cleanup-state-v1`. Both retain their unchanged
64 KiB role-specific payload limits, operations, errors, and no-public-mutation rejection proofs.
They add no listener, TLS, bearer-token processing, `TokenReview`, deployed client, Kubernetes token
projection, or generic backend.

Execution-composition steps 1 through 6 were completed as separate reviewed tranches: the transport
contract was locked, its shared private TLS/TokenReview foundation was implemented, both controllers
used concrete role-specific remote adapters without system-state or tombstone-key inputs, the system
durably fixed and append-registered the exact runner/staging ownership inventory before execution
eligibility, and four one-shot native stagers enforced exact immutable destination schemas,
server-derived grant authorization, stable private input lanes, and create/conflict-observe-only
behavior. GKE documentation consulted at that time said the managed Secret Manager add-on mounted
pinned versions read-only but did not synchronize Kubernetes Secrets, so the old route would have
required explicit fixed-purpose stagers and private render-time provider-version evidence. Its
uncompleted order was recorded as follows; these steps may not now be executed:

1. render the complete projected tokens, exact RBAC, NetworkPolicies, controller and stager
   workloads, runner resources, private stager-result channel, and removal of superseded
   system/controller IAM; and
2. rebuild and scan the exact image, replace and review the offline fixture and evidence lock, then
   request separate Gate 2 authorization.

Under that historical route, no shared SQLite filesystem, copied database, union controller
identity, manifest-only shortcut, or combined silent tranche would have been accepted. KAP-0070
supersedes the route and does not inherit this work list or Gate 2 request.

## Scope checkpoint

The accepted offline tranches established that the locked state, controller, ownership, and staging
architecture could be expressed without widening KAP-0038 or inventing generic provider/storage
interfaces. Completing rendered GKE composition, live enforcement, failure recovery, and public
exposure would have remained a substantial hosted-infrastructure program rather than a clean local
release slice, but that program is retired.

KAP-0053 paused at this checkpoint. It stated that any continuation decision would have needed to
confirm the technical value, proportionality, explicit authorization, and operating budget, and that
sunk implementation cost was not authorization to proceed. KAP-0069 subsequently chose neither
resume nor completion: it superseded this route in favor of KAP-0070.

This checkpoint did not change accepted contracts or make incomplete fixtures deployable. It removed
KAP-0053/KAP-0050 completion as a prerequisite for the v0.2 beta owned by
[`docs/V0.2.md`](../docs/V0.2.md). Its former Gate 2–4 order is retained below only as history and
no longer applies as an actionable continuation sequence.

The earlier increment added `gate2-system-workload.json`, one explicitly incomplete rendered
namespace, service account, headless private Service, and singleton StatefulSet for the implemented
native API, private handoff, and periodic-retention roles. The custom evidence wrapper was not
directly applicable as a Kubernetes List; a later reviewed renderer under that route would have had
to consume its digest-locked objects. Its labels and locked rendered-object digest followed
`sandbox-policy-v2`, and its status named the locked separate-controller topology plus every
then-missing control. All three runtime containers shared only the system-state claim and read-only
tombstone-key projection, disabled ambient service-account tokens, run under the same confined
non-root Pod, and expose only the two named cluster-internal discovery ports. Without the
still-missing NetworkPolicy, that discovery Service is not an access-control boundary. The fixture
rejects added runner authority, public service types, ambient tokens, retention transport, writable
key projection, or undeclared objects. Scheduler, cleanup controller, NetworkPolicy, Secret Manager
staging, Workload Identity binding, provider rendering, and complete role manifests remain explicit
non-claims and blockers.

## Historical planned result (not achieved)

The packet planned for one exact `kapsel-sandbox` revision to run both fixed scenarios on dedicated
non-consequential Kubernetes infrastructure with measured isolation, reconnect, saturation, restart,
receipt, retention, and forced-cleanup evidence, then become ready for bounded public traffic and
independent website integration. It never achieved or authorized that result.

## Historical planned work (do not execute)

The following list preserves the superseded plan and is not a current instruction:

1. Select infrastructure that explicitly supports the native Rust process, durable storage,
   networking, and Kubernetes isolation requirements. Record rejected alternatives and unknowns.
2. Provision a dedicated sandbox cluster and server-owned fixed images. Give each run a uniquely
   owned namespace, narrow service account, quotas, network restrictions, deadline, and cleanup
   identity.
3. Deploy durable admission, scheduler, runner, receipt storage, key custody, bounded observability,
   rate limits, concurrency cap, and global admission stop from reproducible configuration.
4. Prove healthy and unavailable-image runs, reconnect and replay, duplicate submission, saturation,
   expiry, runner restart at each owned seam, cleanup retry, and the global stop.
5. Prove that one run cannot read, mutate, project, or clean another run's resources or receipt.
6. Review public events, receipts, logs, metrics, traces, and retained artifacts for secrets,
   private identifiers, uncontrolled provider bodies, and unnecessary visitor data.
7. Measure startup, run duration, cleanup duration, resource use, retained bytes, and bounded cost
   at the configured concurrency ceiling.
8. Exercise deployment rollback and service unavailability without corrupting durable runs or
   promoting transport failure into a receiver result.
9. Publish only the exact versioned sandbox contract, service revision, bounded public endpoint, and
   approved technical facts needed by the website consumer.

## Historical planned acceptance (not achieved)

These criteria were never completed and do not authorize work or traffic:

- Both fixed scenarios reach classifier-complete receipts through the public interface.
- Browser or client disconnect and runner restart recover the same operation without a blind second
  mutation.
- Cross-run isolation and ownership-safe cleanup pass adversarial tests.
- Saturation rejects before dispatch and reports bounded retry guidance.
- The global stop prevents new admission while preserving recovery and inspection of durable runs.
- Cleanup is forced only after a terminal report and receipt handoff, pre-attempt rejection, or
  setup failure durably known to precede `Application` invocation. Timeout and abandoned-client
  paths preserve receiver resources and drive same-operation recovery first; cleanup remains
  independently observable from operation classification.
- Public output and retained operations data pass threat and privacy review.
- Deployment, rollback, live integration, and cleanup commands are reproducible from an exact clean
  revision.
- A residual-risk review explicitly approves bounded public traffic before the website points to the
  endpoint.

## Non-goals

No production workload, customer cluster, arbitrary request, account system, operator console,
billing, broad dashboard, second capability, managed-cloud product, or v1 support promise.

## Closure

No KAP-0053 work remains. Follow only [KAP-0073](KAP-0073.md) for sandbox removal. Do not render
this packet's remaining workloads, refresh its provider bindings, request credentials or spend from
its fixtures, provision, push an image, publish an endpoint, change DNS, or send traffic. The live
`kapsel.cloud` and Grafik consumer route is cancelled.

## Residual risk

All live properties remain unproved. Authority Composition's normalized patch model is not evidence
that a real Kubernetes admission implementation enforces the rule, and its static-volume/backup
composition is not evidence of storage-class support, snapshot atomicity, primary fencing, restore
exclusion, or managed key custody. The native process has no public-traffic approval. Its periodic
retention role, authenticated controller-state adapters, and append-only runner UID registration are
accepted offline; the incomplete, non-applicable evidence wrapper is not deployable or deployed. The
fixed stagers are implemented only as offline native create/observe behavior; provider CSI custody,
rendered stager/controller composition, resource creation, and the accepted runner's provider
binding remained unimplemented when the route was superseded. The historical Infrastructure
Enforcement Proof would have had to compose those operations under the selected workload, key, and
storage identities, but it may not resume. A public demonstration remains an abuse and operating-
cost surface; namespace policy may be insufficient against a compromised workload or cluster-level
defect, managed sandbox compatibility and key custody may require interface review, cleanup may
outlive the experiment, and fixed infrastructure costs may exceed a future approved ceiling.
