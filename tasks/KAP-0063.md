<!-- work-id: KAP-0063 -->

_Track: v0.2 release · Area: acceptance/publication/website handoff · Created: 2026-07-26_

# Accept And Publish The v0.2 Beta

Status: pre-publication acceptance and rehearsal complete for exact candidate
`5fe0407cd5ebd78f386905e33f79e0f712a57835`. A local annotated `v0.2.0` tag is prepared at those
bytes. No push, GitHub release, public asset, or website publication occurred; the maintainer will
push later, and release creation still requires explicit approval.

Problem: a technically complete candidate is not a release until one exact revision, artifact set,
compatibility contract, residual-risk record, publication transaction, and downloaded verification
are independently accepted. The website also needs a claim-complete handoff rather than inferred
repository status.

Contract owners: [v0.2 release design](../docs/V0.2.md), [release artifacts](../docs/RELEASE.md),
[security policy](../SECURITY.md), the accepted KAP-0058 through KAP-0062 evidence, and the exact
KAP-0065 replacement-candidate evidence.

Depends on: exact KAP-0065 replacement candidate and its reconciled qualification, distribution, and
independent-review evidence. Explicit publication authorization remains the gate between accepted
rehearsal and the publication transaction.

## Work

- Reconcile every earlier packet, owner, vector, gate, budget, exception, and residual risk against
  one clean candidate revision.
- Obtain separate architecture, lifecycle/correctness, compatibility, migration/rollback, security,
  privacy, supply-chain, documentation, and release reviews.
- Rehearse tag, hosted build, artifact upload, release notes, verification, withdrawal, and
  replacement procedures without publication.
- After explicit approval, push without force, create the immutable tag/release, and upload only the
  accepted artifacts and sidecars.
- Download public assets in a fresh supported environment and re-run identity, authentication,
  extraction, install, upgrade, CLI/MCP, inspection, demo, cleanup, and uninstall verification.
- Produce a standalone website handoff naming the exact artifact, one product promise, allowed
  claims, required non-claims, supported target, call to beta evaluation, and evidence links.
- Activate KAP-0047 only after public verification succeeds.

## Acceptance

- Every review addresses the exact candidate and reports no unresolved blocker.
- The tag, release metadata, archive, sidecars, source revision, compatibility owner, and release
  notes agree exactly.
- Public assets match accepted local bytes and pass downloaded-artifact verification.
- `v0.1.0` and `v0.1.1` tags/assets remain unchanged.
- The website handoff can stand alone without private context or unpublished repository state.
- Public wording says developer beta, one Kubernetes operation, no production readiness, no
  exactly-once effect, explicit `UNKNOWN`, `INSPECTED` not `VERIFIED`, and sole supported target.
- KAP-0047 names the exact published beta as its evidence baseline.

## Non-goals

No automatic publication, crates.io, production launch, public sandbox, customer identity, pricing,
market conclusion, second capability, daemon, managed control plane, or post-beta implementation
backlog.

## Pre-publication evidence

- Architecture, lifecycle/correctness, compatibility, migration/rollback, security, privacy,
  supply-chain, documentation, and release reviews independently accepted commit
  `5fe0407cd5ebd78f386905e33f79e0f712a57835`, tree `a292c100311f996494b9d7bff91c9a0d4d96b14c`, and
  archive SHA-256 `82db38b05772b1b6a4ef9a9312baf9633f0ce652ddfe8052ed7df86d1fd1d969` with no
  unresolved blocker. Documentation reconsideration confirmed that candidate-time task status is
  historical external evidence, tasks are absent from the archive, and no canonical or bundled
  contradiction requires new bytes.
- Recomputed downloaded hashes, strict manifests, and fresh connected Cosign verification passed.
  The signed `SHA256SUMS` SHA-256 is
  `8eab8fe572c68e483301f3c33dfaf2404599c378ee160fd6b6f224639a90dbb2`; the Sigstore bundle SHA-256 is
  `78e4d3cbcff7ceb2028ecc5cfb36ebc7a6c26c4f6dac649207bf2658e7a0be24`.
- Downloaded-artifact smoke and exact `v0.1.1` upgrade, backup, retained inspection,
  rollback/reopen, and downgrade passed in the pinned x86-64 Debian 12 smoke container. The
  extracted downloaded-artifact live-kind demonstration already passed candidate run
  [30410695345](https://github.com/kapsel-cloud/kapsel/actions/runs/30410695345); the local
  source-owned demonstration also passed and cleaned its disposable cluster.
- Prior `v0.1.0` and `v0.1.1` annotated tag targets and public asset digests were snapshotted and
  remained unchanged. No remote `v0.2.0` tag or release existed during rehearsal.
- Local annotated tag `v0.2.0` object `8e9188d53b412ab14fa1459d8e949b1fb17f8feb` peels to the exact
  candidate. It was not pushed. The prepared release transaction uses exactly the archive, adjacent
  checksum, SPDX SBOM, signed digest manifest, and Sigstore bundle; it creates a GitHub prerelease
  that is not latest and never rebuilds, renames, overwrites, or re-signs accepted bytes.
- Release notes and a standalone website claim handoff preserve developer-beta, one-operation,
  sole-target, no-production, no-exactly-once, explicit `UNKNOWN`, and `INSPECTED`-not-`VERIFIED`
  limits. The website handoff remains unpublished until public downloaded verification succeeds.

## Next action

The maintainer may push the evidence commit and local `v0.2.0` tag without force. Before creating a
GitHub release, repeat remote tag peel, connected Cosign verification, accepted hashes,
prior-release identity comparison, and collision checks, then stop for explicit release-creation
approval. After publication, download and verify the public bytes on a fresh supported host before
publishing the website handoff or activating KAP-0047.

## Residual risk

A polished beta and website can attract evaluation but cannot guarantee adoption or income. The
post-release evidence gate decides whether Kapsel deepens, adds one coherent workflow operation,
starts a resident pilot, freezes, or retires.
