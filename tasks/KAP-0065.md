<!-- work-id: KAP-0065 -->

_Track: v0.2 release · Area: documentation/candidate replacement · Created: 2026-07-28_

# Correct And Replace The v0.2 Candidate

Status: complete and independently reviewed. The accepted KAP-0062 candidate remains immutable but
is superseded for release consideration. Exact replacement candidate
`5fe0407cd5ebd78f386905e33f79e0f712a57835` passed native qualification, authenticated production,
downloaded verification, and correction acceptance. KAP-0063 may resume independent acceptance and
rehearsal; publication remains separately gated.

Problem: the exact KAP-0062 candidate is technically qualified, but its canonical and bundled prose
still describes completed v0.2 implementation, qualification, and candidate production as planned or
incomplete. KAP-0063 cannot accept or publish bytes whose documentation contradicts the candidate
evidence.

Contract owners: [v0.2 release design](../docs/V0.2.md), [architecture](../docs/ARCHITECTURE.md),
[release artifacts](../docs/RELEASE.md), [build](../docs/BUILD.md),
[security policy](../SECURITY.md), and the prospective [V1 technical direction](../docs/VISION.md).

Depends on: immutable KAP-0062 candidate evidence and the explicit decision to replace rather than
silently mutate that candidate.

## Work

- Correct `docs/V0.2.md` first so the canonical release owner distinguishes completed candidate work
  from KAP-0063 acceptance and publication work.
- Align `README.md`, `docs/ARCHITECTURE.md`, `SECURITY.md`, and `docs/BUILD.md` with that owner;
  preserve `v0.1.1` as the current published artifact until publication succeeds.
- Distinguish supported caller interfaces, distribution and demonstration interfaces, repository
  tooling, and qualification evidence without widening the KAP-0038 request.
- Put active v0.2 release commands before paused sandbox commands in active guides and clearly mark
  the sandbox as excluded from the release unit.
- Update `docs/VISION.md` narrowly to record the prospective v1 tooling posture: normal
  installation, operation, recovery, migration, inspection, health, and upgrade must not
  accidentally require Python or shell; repository assurance tooling remains implementation-private,
  and any future public demo dependency requires an explicit distribution decision.
- Record the reviewed Rust public-item inventory as packet evidence without changing Rust
  visibility, lifecycle behavior, receipt shape, or package seams.
- Reconcile the complete delta against KAP-0061 invalidation rules, rerun every affected lane, and
  bind the accepted qualification evidence to the exact replacement revision.
- Repeat KAP-0062 clean assembly, reproducibility, SBOM and vulnerability review, publisher
  authentication, downloaded-artifact verification, upgrade/rollback proof, and independent review
  for one newly named candidate.
- Retain the old candidate bytes, digests, bundle, and evidence unchanged and record them as
  superseded rather than withdrawn for compromise.

## Reviewed architecture inventory

The correction decision measured 40,967 tracked Rust lines, 9,004 Python lines, and 799 shell lines.
The 25 Python and five shell files are predominantly release, qualification, migration, sandbox,
documentation, and demonstration assurance code rather than ordinary product runtime. The installed
ordinary executable does not require Python or shell; the v0.2 demonstration and evaluator route do
name them as distribution prerequisites.

The root release package remains deep around `Application`; deleting it would spread authorization,
lifecycle sequencing, recovery, classification, and receipt publication across CLI, MCP, and the
real sandbox consumer. `transport_support` and the private Journal opening/schema children also pass
the deletion test. The reviewed interface debt is narrower: `AgentRequest` is a shallow alias,
workspace visibility must be mapped to real consumers, and `OperationReport` receipt-path exposure
requires grilling before change. KAP-0068 owns those post-release decisions.

The paused sandbox contains 22,819 tracked Rust lines and a broad library interface spanning several
independent change axes. It remains excluded from v0.2 release-source cleanup; KAP-0069 owns an
explicit continuation, preservation, or retirement decision. Repeated release and qualification
invariants across Python programs pass the deletion test for future deep tooling modules in the
existing unpublished `kapsel-dev` package; KAP-0066 and KAP-0067 own that work.

## Acceptance

- Canonical, public, and bundled documentation describe the implemented v0.2.0 design without
  inferring candidate acceptance, currentness, or publication from source or package identity;
  ordered authenticated evidence owns exact release state.
- Completed implementation, compatibility, migration, qualification, and distribution work is not
  described as future work; acceptance and publication remain explicitly future and separately
  authorized.
- Active release guidance no longer gives paused sandbox work equal or earlier prominence.
- The v1 tooling posture prevents Python or shell from becoming an accidental installed-runtime
  dependency without converting repository tooling during this packet.
- No Rust, Python, shell, workflow, dependency, capability, lifecycle, receipt, CLI, MCP, migration,
  or sandbox behavior changes.
- Formatting, links, focused terminology searches, privacy and documentation review, default CI,
  every invalidated qualification lane, exact candidate production, and downloaded verification pass
  against the replacement revision.
- Fresh correction and replacement-candidate reviews find no blocker, and the exact evidence is
  sufficient for KAP-0063 to resume independent acceptance and rehearsal.

## Non-goals

No broad documentation rewrite, Python-to-Rust port, release-tooling redesign, qualification-tooling
redesign, Rust interface change, sandbox decomposition, generic provider or transport interface,
second capability, tag, release, website publication, or post-release evidence collection.

## Produced evidence

- Replacement candidate commit `5fe0407cd5ebd78f386905e33f79e0f712a57835` and tree
  `a292c100311f996494b9d7bff91c9a0d4d96b14c` change no Rust, Python, shell, workflow, dependency,
  vector, test, capability, lifecycle, receipt, CLI/MCP, migration, target, or sandbox behavior.
- Exact CI run https://github.com/kapsel-cloud/kapsel/actions/runs/30409979931 passed the default,
  x86-64 artifact, and informational coverage jobs.
- The native x86-64 Linux closed qualification rerun passed all 11 required lanes, 43 budgets, and
  54 results with zero failures. The replacement manifest SHA-256 is
  `e72559469a1dfd7f4b496bd59be783fd0c1a880caa8139af2871719252785341`; its production-source and
  ordinary/demo executable identities remain unchanged from the superseded candidate.
- Candidate run https://github.com/kapsel-cloud/kapsel/actions/runs/30410695345 passed authenticated
  production and downloaded-candidate verification. Archive SHA-256 is
  `82db38b05772b1b6a4ef9a9312baf9633f0ce652ddfe8052ed7df86d1fd1d969`, signed `SHA256SUMS` SHA-256 is
  `8eab8fe572c68e483301f3c33dfaf2404599c378ee160fd6b6f224639a90dbb2`, and Sigstore bundle SHA-256 is
  `78e4d3cbcff7ceb2028ecc5cfb36ebc7a6c26c4f6dac649207bf2658e7a0be24`.
- Two-build reproducibility, fresh SBOM/Trivy review, connected and captured-root Cosign
  verification, negative identity/manifest checks, authenticated-time verification, extracted-only
  smoke, disposable-kind demonstration, and exact v0.1.1 upgrade/rollback/downgrade passed.
  Downloaded manifests and all nine candidate-evidence file hashes matched locally.
- Fresh final review found no correction, scope, compatibility, evidence-identity, or authorization
  blocker. No `v0.2.0` tag, GitHub release, public asset, or website publication was created.

## Next action

Hand only the exact replacement candidate and evidence above to KAP-0063 for independent
pre-publication acceptance and rehearsal.

## Residual risk

Documentation-only source changes still alter the Git tree, archive bytes, metadata, SBOM,
checksums, signed manifest, and publisher event. Reusing old candidate evidence without explicit
invalidation reconciliation would falsely bind review to different bytes. Narrow correction also
does not resolve repository-tooling or paused-sandbox maintenance debt; KAP-0066 through KAP-0069
own those later decisions.
