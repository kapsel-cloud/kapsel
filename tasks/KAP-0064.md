<!-- work-id: KAP-0064 -->

_Track: v0.2 implementation · Area: architecture/locality · Created: 2026-07-27_

# Deepen Release-Source Locality

Status: complete and independently reviewed. The root package retains one deep Journal interface
while exact schema/migration and owner-private opening/backup/filesystem implementation now have
explicit private locality.

Problem: the root release package remains one deep product module, but migration and hardening have
concentrated independently changing schema/migration, backup/filesystem, lifecycle, receipt, and
test implementation in several long source files. Raw line count is not itself a defect, yet v0.2
should finish source-locality work before qualification freezes a baseline and later structural
churn forces expensive evidence reruns.

Contract owners: [architecture](../docs/ARCHITECTURE.md), [v0.2 release design](../docs/V0.2.md),
KAP-0038, and [V1 technical direction](../docs/VISION.md).

Depends on: accepted KAP-0060 migration, backup, restore, rollback, and downgrade behavior.

## Work

- Inventory root `kapsel` release-source files by owner, independent reason to change, production
  implementation size, test size, and current interface; do not use a line-count threshold as the
  decision rule.
- Keep `Application` and the gateway as deep modules while moving independently changing journal
  schema/migration and backup/filesystem implementation into clearly owned private child modules
  where the KAP-0060 result proves those concerns separable.
- Reassess gateway orchestration, receipt encoding/inspection, publication, and the concrete
  Kubernetes adapter; split only where doing so improves locality behind an existing narrow
  interface rather than creating pass-through modules.
- Keep owner-specific invariant tests adjacent and navigable, whether inline or in private test
  children, without extracting production seams solely for test convenience.
- Preserve compile-time composition, one concrete Kubernetes adapter, private SQL and journal shape,
  exact retained vectors, durable transition ordering, and all supported CLI/MCP behavior.
- Update `ARCHITECTURE.md` only for implementation ownership or dependency-direction changes that
  survive the refactor.

## Acceptance

- Each changed file has one explainable owner and no unrelated independent change axis that should
  live behind an existing private interface.
- The journal retains one private interface while schema/migration and backup/filesystem behavior
  have explicit implementation locality; callers cannot select storage or sequence durable states.
- Gateway lifecycle orchestration and receipt construction/inspection remain deep rather than being
  spread across shallow pass-through modules.
- Exact `v0.1.1` migration, backup, restore, downgrade, grant, and receipt vectors remain
  state-for-state and byte-for-byte accepted.
- No public Rust compatibility promise, crate, generic storage/provider interface, runtime plugin,
  second adapter, capability, or sandbox dependency appears.
- Focused lifecycle, migration, receipt, CLI/MCP, Clippy, formatting, default gate, and fresh
  architecture/locality review pass before KAP-0061 freezes its qualification baseline.

## Non-goals

No sandbox decomposition, arbitrary maximum file length, behavior redesign, SQL compatibility
promise, migration-policy change, receipt-format change, performance qualification, packaging,
version bump, or publication.

## Accepted inventory

The accepted inventory uses ordinary-build/test-only physical line counts only to describe source
mass, never as an extraction threshold. Test-only counts include dedicated test files and every
`#[cfg(test)]` item or statement with its body; interface, owner, and independent reason to change
selected each decision:

| Source                                                                           |            Prod/test lines | Owner and interface                                                                                 | Locality decision                                                                                             |
| -------------------------------------------------------------------------------- | -------------------------: | --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `src/lib.rs`; `src/main.rs`                                                      |                24/14; 30/0 | Workspace-visible interface map; process arguments, streams, and exit                               | Retain compact composition; no release-package seam                                                           |
| `src/application/mod.rs`                                                         |                      355/0 | Deep `Application` execution/reconciliation and operator composition                                | Retain; KAP-0059 already concentrated lifecycle sequencing                                                    |
| `src/command/mod.rs`; `src/mcp.rs`; `src/transport_support.rs`                   |        657/0; 555/0; 271/0 | CLI grammar; MCP framing; shared private operator loading/projection                                | Retain separate transport interfaces around the earned shared private seam                                    |
| `src/gateway/mod.rs`                                                             |                    733/132 | Deep Gateway lifecycle orchestration, concrete request vocabulary, errors, and validation           | Retain cohesive; splitting orchestration would spread lifecycle ordering through shallow modules              |
| `src/gateway/authorization.rs`; `src/gateway/demo_control.rs`                    |               259/73; 68/0 | Exact grant verification; compile-time demonstration fault control                                  | Retain owner-local policy and separately gated test control                                                   |
| `src/gateway/journal/mod.rs`                                                     |                     808/77 | One Journal interface: rows, snapshots, worker locking, capacity, receipts, and guarded transitions | Retain deep interface; callers still cannot choose SQL, storage, or lifecycle steps                           |
| `src/gateway/journal/schema.rs`; `src/gateway/journal/opening.rs`                |             557/0; 419/152 | Exact layout/legacy migration; durable entry, backup verification, rollback recovery, Unix identity | Extract as private children; each hides hundreds of independently changing lines behind a small interface     |
| `src/gateway/kubernetes/mod.rs`; `adapter.rs`; `facts.rs`                        |      7/0; 258/585; 216/159 | One concrete Deployment image adapter; bounded receiver facts/classification                        | Retain cohesive; one production adapter does not earn another seam, and test mass is not production mass      |
| `src/gateway/receipt/mod.rs`; `receipt/publication.rs`                           |           941/295; 303/176 | Classifier-complete exact encoding/inspection; descriptor-relative immutable publication            | Retain: wire construction and parsing share one exact-format owner; publication already has distinct locality |
| `src/gateway/tests/mod.rs`; `support.rs`                                         |                0/43; 0/321 | Private interface-level test composition and deterministic support                                  | Retain shared private test vocabulary; no production seam was extracted for tests                             |
| `src/gateway/tests/lifecycle.rs`; `migration.rs`; `recovery.rs`; `validation.rs` | 0/331; 0/443; 0/288; 0/316 | Owner-specific transition, schema/opening, process-loss, and input invariants                       | Retain interface-level evidence near the gateway test owner                                                   |
| `src/gateway/tests/receipt.rs`; `publication.rs`; `v011_upgrade.rs`              |        0/583; 0/87; 0/1587 | Receipt/publication invariants and exact provenance-bound upgrade matrix                            | Retain evidence; fixture breadth and subprocess orchestration do not justify production interfaces            |
| `src/kind_tests.rs`; `src/simulation_tests.rs`                                   |               0/438; 0/355 | Separately selected live-kind and seeded simulation lanes                                           | Retain test-only lanes; no release-source package extraction                                                  |

The deletion test confirms both new children earn their place: deleting either moves substantial
cohesive schema knowledge or filesystem/backup/recovery knowledge back into Journal opening.
Deleting receipt, Gateway orchestration, or the concrete adapter candidates would instead spread one
owner’s invariants or create pass-through modules, so they were deliberately not split.

## Evidence

- Commit `d512b72` makes `opening` and `schema` private children. The only opening interface returns
  the constructed connection and worker lock; the schema child exposes only marker identity,
  initialization, integrity recognition, and supported-layout recognition to its sibling. There is
  no trait, runtime selection, public SQL, new crate, provider/storage adapter, or reverse package
  dependency.
- Dependency direction is `Gateway -> Journal -> opening -> schema`. Journal callers retain the same
  `Journal::open` and guarded transition interface; production code outside the journal tree cannot
  name either child.
- The exact KAP-0060 source-fixture gate passed all nine v0.1.1 states, 54 migration/restore process
  kills, hot rollback, complete row/provider/backup/receipt comparison, candidate reopen, and exact
  old-source downgrade reopen after the move.
- Focused migration, lifecycle, recovery, grant-v1, receipt-v2, CLI, and MCP tests passed. Root
  all-target Clippy, formatting, links, diff hygiene, the sandbox package/deletion boundary gate,
  and the complete default gate passed.
- Fresh architecture/locality review found the two children deep, deletion-test positive, private,
  one-way, and behavior-preserving. It confirmed that Gateway orchestration, receipt
  encoding/inspection, publication, receiver facts, and the concrete Kubernetes adapter retained
  cohesive ownership with no new production consumer of the existing test-visible connection.

## Next action

Hand the accepted, structurally stable root release source to [KAP-0061](KAP-0061.md) for finite
qualification and baseline freezing.

## Residual risk

Private moves can still hide coupling that unchanged behavior tests do not expose, and future
schema, receipt, or capability growth may earn different locality. This packet proves current source
ownership, not an immutable module tree, downloaded-artifact continuity, sudden-power/storage
behavior, performance qualification, or another adapter.
