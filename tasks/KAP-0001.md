<!-- work-id: KAP-0001 -->
_Track: V1 · Area: repo/tasks · Created: 2026-07-07_

# Bootstrap Revival Task Board

Problem: Kapsel needs a durable task board before the witness pivot starts so future agents do not
rediscover the conversation or re-plan the V1 sequence from scratch.

Acceptance:

- Create tracked task packets for the full path from current webhook code to V1 release.
- Create ignored todo.txt with exactly one active cursor and queued work.
- Ignore local agent and task state such as todo files, tasks/local, and .pi-subagents.
- Order tasks so proof contracts and verifier-first work precede collector and witness services.
- Include explicit review and cleanup gates, including a final no-legacy sweep.
- Require webhook-alpha tag creation before destructive legacy cleanup can start.
- State that tasks are planning aids; proof contracts live in owner docs, not task packets.

Verification:

- Inspect git diff for tasks, todo.txt, and .gitignore.
- Confirm todo.txt is ignored and tasks/KAP-*.md are tracked candidates.
- Review final task order against the V1 design and follow-up corrections.

Source anchors:

- `Current V1 design conversation`
- `Myran workflow pattern, local reference`

## Last action

Created the initial KAP task board. On 2026-07-10, subsequent review replaced both the proprietary
witness plan and the over-corrected refund-product plan with a lean, profile-oriented evidence V1.
The active board now separates reusable technical V1 from concurrent commercial discovery and keeps
the synthetic reference profile independent from commercial profile selection.

## Next action

Start KAP-0002: freeze the corrected profile-oriented V1 charter before implementation.
